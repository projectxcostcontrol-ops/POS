# Rankrua - งานที่ค้างไว้ / ตัดสินใจแล้วแต่ยังไม่ทำ

## 1. เก็บสำเนายอดขายไว้ใน Firestore (ยังไม่ทำ)

**ปัญหา:** ตอนนี้หน้า "รายการบิล" ดึงสดจาก Loyverse ทุกครั้ง ไม่ได้เก็บสำเนา
พอบิลเกิน 30 วัน มันจะหายไปตามข้อจำกัดแผนฟรีของ Loyverse

สิ่งที่เก็บถาวรอยู่แล้ว (ไม่หาย):
- movement ledger (การตัดสต๊อกทุกครั้ง พร้อมเลขบิลอ้างอิง)
- ประวัติต้นทุนจากการรับของ
- การนับสต๊อกทุกรอบ
- รายรับรายจ่ายที่บันทึกเอง
- สูตร / วัตถุดิบ / หมวดหมู่

สิ่งที่ไม่ได้เก็บ:
- ตัวบิลเอง (ยอดเงิน รายการที่ขาย จำนวน)
- `processed_receipts` เก็บแค่ "เลขบิลนี้ประมวลผลแล้ว" กันตัดสต๊อกซ้ำ
  ไม่ได้เก็บรายละเอียด

**ผลกระทบ:** เดือนหน้าจะดูไม่ได้ว่า "เดือนนี้ขายได้เท่าไหร่ กำไรเท่าไหร่"
ทั้งที่ข้อมูลสต๊อกยังอยู่ครบ

**ทางแก้ที่ตกลงกันไว้:** ตอน sync อ่านบิลมาอยู่แล้ว ให้บันทึกสรุปเก็บเพิ่ม
(วันที่ เลขบิล ยอดรวม รายการที่ขาย จำนวน) ต้นทุนแทบไม่เพิ่ม

ได้ประโยชน์:
- ดูยอดขายย้อนหลังได้ไม่จำกัด แม้ Loyverse ลบไปแล้ว
- คำนวณกำไรขั้นต้นรายเดือนย้อนหลังได้
- ไม่ต้องจ่ายค่า Unlimited sales history ของ Loyverse
- ตอนย้ายไป POS ของตัวเอง ข้อมูลเก่ายังอยู่กับเรา

ปริมาณข้อมูล: ร้านขาย 100 บิล/วัน ≈ 36,000 บิล/ปี ยังอยู่ในโควต้าฟรีของ Firestore

---

## 2. แจ้งเตือน LINE (3.5) - ยังไม่ทำ

**สถานะ:** LINE Notify ปิดบริการไปแล้ว (31 มี.ค. 2025) ต้องใช้ LINE Messaging API
ผ่าน LINE Official Account แทน ซึ่งตั้งค่ายากกว่าเดิมมาก

**ยังไม่ได้ตัดสินใจ 3 ข้อ:**
1. ใครเป็นเจ้าของ LINE OA
   - แบบ ก: เราสร้าง OA เดียว ลูกค้าแอดเป็นเพื่อน (ลูกค้าง่าย แต่เราจ่ายค่าส่ง)
   - แบบ ข: ลูกค้าสร้าง OA เอง (ลูกค้าจ่ายเอง แต่ตั้งค่ายากมาก)
2. ส่งเข้ากลุ่ม LINE ของร้าน หรือส่งหาเจ้าของคนเดียว
3. เริ่มจากแบบไหนก่อน

**หลักการที่ตกลงไว้:** เตือนเฉพาะเรื่องที่ต้องลงมือทำวันนี้ และรวมเป็นข้อความเดียว
- เตือน: สต๊อกต่ำ (สรุปวันละครั้ง), สต๊อกติดลบ, variance เกินเกณฑ์, sync ล้มเหลว
- ไม่เตือน: ยอดขายทุกบิล, ทุกครั้งที่สแกนใบส่งของ, ยอดขายรายวันปกติ

---

## 3. POSPOS / Ocha adapter - รอลูกค้าจริง

- **POSPOS:** มี Developer API (Transaction/Stock/Member) แต่อยู่ในแพ็กเกจเสียเงิน
  ทำ adapter ได้ (ports-and-adapters รองรับอยู่แล้ว) แต่ต้องมี API doc จริง
  + บัญชีทดสอบก่อน ไม่งั้นเขียนแบบเดาแล้วพังตอนต่อจริง
- **Ocha:** หา public API ไม่เจอ แม้แต่ FlowAccount ยังต้องให้ผู้ใช้
  ดาวน์โหลด CSV/Excel มา import ถ้าจะทำต้องเป็นระบบอัปโหลดไฟล์
  ซึ่งเสียจุดขายเรื่องตัดสต๊อกอัตโนมัติไป

**ตกลงกันไว้:** รอจนมีลูกค้าจริงที่ใช้ POSPOS ก่อนค่อยทำ

---

## 4. ข้อจำกัดที่รับทราบแล้ว (ไม่ใช่บั๊ก)

- **Loyverse แผนฟรี ดูย้อนหลังได้ 30 วัน** - ระบบขอ 30 วันเพื่อเลี่ยงขอบ 31 วัน
  ถ้าต้องการมากกว่านี้ต้องสมัคร Unlimited sales history ที่ Loyverse
- **ซิงก์ครั้งแรกของสาขาได้ 0 บิลเสมอ** - ตั้งใจให้เป็นแบบนั้น
  (ตั้งจุดเริ่มที่ "ตอนนี้" ไม่ย้อนหลัง) รอบถัดไปถึงเริ่มดึงจริง
- **สูตรที่ไม่ใส่เครื่องปรุง** ต้นทุนต่อจานจะต่ำกว่าจริงเล็กน้อย
  และ variance จะไม่นำเครื่องปรุงมาเทียบ - ตัดสินใจให้ผู้ใช้เติมเอง
- **รอบนับสต๊อกแรกยังวิเคราะห์ variance ไม่ได้** ต้องมีรอบที่สองถึงเทียบได้

---

## 5. หน้าของในครัว - ยังไม่ได้ทำ (ออกแบบเสร็จแล้ว)

หน้าที่พนักงานเปิดบ่อยสุด ตอนนี้ยังต้องดูตัวเลขแล้วตีความเองว่าเหลือน้อยหรือยัง

### แบบที่ตกลงไว้

```
ของในครัว · 24 อย่าง              [+ เพิ่มของ]
[ค้นหา____________]

⚠ ใกล้หมด 3 อย่าง — ควรสั่งเพิ่ม     <- สรุปบนสุด

  กุ้งขาว                    0.8 กก.
  เหลือน้อย ควรสั่งเพิ่ม
  ▓▓░░░░░░░░ (แดง)
              [ประวัติ] [แก้ไขจำนวน] [แก้ไข]

  น้ำมันพืช                    3 ขวด
  ใกล้ถึงจุดสั่งซื้อ
  ▓▓▓▓░░░░░░ (เหลือง)

  ข้าวสาร                     18 กก.
  เพียงพอ
  ▓▓▓▓▓▓▓▓░░ (เขียว)
```

### สิ่งที่ต้องทำ
- เพิ่มคำบอกสถานะเป็นตัวอักษร ไม่ใช่ให้ดูตัวเลขแล้วตีความเอง
- แถบสีบอกระดับ (แดง/เหลือง/เขียว)
- ของใกล้หมดขึ้นบนสุดเสมอ + สรุปจำนวนด้านบน
- คงปุ่ม "แก้ไขจำนวน" กับคำเตือนเรื่องไม่นับเป็นส่วนต่างไว้เหมือนเดิม

### จุดที่ต้องระวัง - วัตถุดิบที่ยังไม่ได้ตั้งจุดสั่งซื้อ

แถบสีทำงานได้ต้องมี par_level ตั้งไว้ก่อน ถ้ายังไม่ได้ตั้ง **ห้ามแสดงเป็น
สีเขียว/"เพียงพอ"** เพราะจะทำให้เข้าใจผิดว่าของพอทั้งที่ระบบไม่รู้

ต้องแสดงแยกเป็นสถานะที่สี่ เช่น "ยังไม่ได้ตั้งจุดสั่งซื้อ" (สีเทา) พร้อม
ทางลัดให้ตั้งได้เลย - ไม่งั้นคำเตือน "ของใกล้หมด" ในหน้าแรกจะไม่ครบ
โดยไม่มีใครรู้

### หมายเหตุ
- `/alerts` ใช้ par_level เป็นเกณฑ์อยู่แล้ว (ดู build_alerts ใน sales_report.py)
  ถ้าวัตถุดิบไหนไม่มี par_level มันจะไม่เข้าเงื่อนไขเตือนเลย - สอดคล้องกัน

---

## 6. งาน UI ที่ทำแล้ว (รอส่งรวมทีเดียว)

- ยอดขาย -> **รายการขาย**
- รายรับรายจ่าย: ยุบ 3 แท็บเป็นหน้าเดียว เลือกหมวดตอนบันทึกแทน
  (ค่าวัตถุดิบไม่มีให้เลือก เพราะระบบคำนวณเองจากสูตร x ยอดขาย
   ถ้าให้กรอกจะนับซ้ำกับที่คำนวณไว้)
- สูตรอาหาร: เปลี่ยนคำว่า AI -> ระบบ, ไอคอน 🪄 -> ⚡,
  "+ สร้าง" -> "+ เพิ่มวัตถุดิบใหม่" (ใช้ MaterialPicker ค้นหาได้)
- ตั้งค่า: เอาการ์ด "ตรวจสอบยอดขาย" ออก
  -> รวมเป็นปุ่ม "↻ อัปเดตข้อมูล" ปุ่มเดียวในหน้าแรก
     (sync -> ตรวจ -> ถ้าขาดก็ซ่อมเอง -> ตรวจซ้ำ)
- นับของ + ของหายไปไหน รวมเป็นหน้าเดียว (VarianceSummary card)
- MaterialPicker: ค้นหา + สร้างวัตถุดิบใหม่ในฟอร์มได้เลย
  ใช้ทั้งหน้าซื้อของ หน้าตรวจร่าง และหน้าสูตรอาหาร

- เมนูข้าง: ยุบหัวข้อ (ทุกวัน/ทุกสัปดาห์/ดูย้อนหลัง) เป็นรายการเดียว
  "ตั้งค่าครั้งเดียว" -> "ตั้งค่าระบบ" กดแล้วขยาย (เปิดเองถ้าอยู่ในหน้านั้น)
  หน้า "ตั้งค่า" ข้างในเปลี่ยนชื่อเป็น "เชื่อมต่อ & สาขา" กันสับสนกับเมนูนอก
- กราฟ: แกน X คงที่ตามช่วงเวลา (วันนี้ = 0-24 ชม. เสมอ) ไม่ยืดหดตามข้อมูล
  เพิ่มแกน Y + เส้นตาราง + ตัวเลขปัดสวย
  **แก้บั๊ก**: backend จัดกลุ่มเวลาเป็น UTC ทำให้ยอดตอน 2 ทุ่มไปอยู่วันถัดไป
  และกราฟรายชั่วโมงเลื่อน 7 ช่อง -> ตอนนี้ frontend ส่ง tz_offset ไปด้วย

### ยังเหลือ
- หน้าของในครัว (ดูข้อ 5)
- ตัวนำทางตอนเริ่มใช้ครั้งแรก

---

## 7. ปรับความเร็วการอ่าน Firestore (ทำแล้ว)

**ปัญหา:** หน้าแรกช้าเพราะอ่าน Firestore เยอะเกินจำเป็น

**แก้ 3 จุด:**

1. `list_sales` ให้ Firestore กรองช่วงวันที่แทนที่จะดึงทั้ง collection
   มากรองใน Python — เดิมดูแค่วันนี้ก็ต้องโหลดบิลทั้งปีมาก่อน
   ทำให้ยิ่งใช้นานยิ่งช้าลงเรื่อย ๆ ทั้งที่ช่วงเวลาเท่าเดิม
   **ต้อง deploy index:** `firebase deploy --only firestore:indexes`

2. รวม `/sales/summary` + `/sales/top-items` เป็น `/sales/overview`
   เดิมหน้าแรกเรียกทั้งคู่พร้อมกัน = อ่าน sales ชุดเดียวกัน 2 รอบ
   บวกช่วงเปรียบเทียบอีก 1 รอบ

3. `all_recipes()` อ่านสูตรทั้งหมดครั้งเดียว แทนที่จะอ่านทีละเมนู
   (เดิม comment เขียนว่าเลี่ยง N+1 แต่โค้ดทำ N+1 อยู่)

**ผลรวม:** ดู "เดือนนี้" 2,000 บิล เดิมอ่าน ~6,000 doc + สูตรทีละเมนู
ตอนนี้อ่านเฉพาะบิลในช่วง 1 รอบ + สูตร 1 รอบ

**ยังไม่ได้ทำ (ถ้ายังช้าอยู่):** เก็บสรุปรายวันตอน sync
(`sales_daily/{date}`) จะทำให้ดูเดือนนี้อ่านแค่ 30 doc
ข้อแลกเปลี่ยน: แก้สูตรทีหลังแล้วกำไรย้อนหลังไม่อัปเดตเอง ต้องมีปุ่มคำนวณใหม่

---

## 7.1 ต้นทุนการ sync (ทำแล้ว - A1/A2)

**ปัญหา:** sync ตอบถูก แต่จ่ายแพงขึ้นเรื่อย ๆ ตามอายุร้าน มองจากข้างนอกไม่เห็น
เห็นอีกทีคือบิล Firebase หรือโควต้าหมดตอนบ่าย

1. `list_processed_receipts()` อ่าน **ทั้ง collection** ทุกรอบ sync เพื่อตอบว่า
   "25 บิลที่เพิ่งดึงมา เคยเห็นหรือยัง" — collection นี้มี 1 doc ต่อ 1 บิลตลอดกาล
   ร้าน 100 บิล/วัน ผ่านไป 1 ปี = 36,000 doc x 288 รอบ/วัน ≈ **10 ล้าน read/วัน**
   (โควต้าฟรี 50,000)
   -> เปลี่ยนเป็น `processed_receipts_among(store_id, numbers)` batch-get
      เฉพาะเลขบิลที่ดึงมาจริงผ่าน `db.get_all()`

2. `_apply` **เขียนทับบิลเดิมทุกรอบ** เพราะ overlap window 6 ชม. ทำให้ทุก 5 นาที
   เขียนบิล 6 ชั่วโมงย้อนหลังใหม่หมดทั้งที่ไม่มีอะไรเปลี่ยน ≈ **7,200 write/วัน/สาขา**
   (โควต้าฟรี 20,000)
   -> เขียนเฉพาะบิลที่ยังไม่เคยบันทึก

**ทำไมข้อ 2 ปลอดภัย:** `_apply` บันทึกก่อนแล้วค่อย mark เสมอ ทุกเส้นทาง
ดังนั้น "mark แล้ว" แปลว่า "บันทึกแล้ว" ถ้าพังกลางคันจะยังไม่ถูก mark รอบหน้าทำใหม่

**ยกเว้น `full=True`** ยังเขียนทับทั้งหมดเหมือนเดิม เพราะเป็นทางซ่อม —
แถวที่บันทึกผิด/ขาดฟิลด์ที่เพิ่งเพิ่มทีหลัง มองจาก "เคยเห็นบิลนี้ไหม" ไม่เจอ
ปุ่ม "อัปเดตข้อมูล" หน้าแรกยังทำ sync -> reconcile -> repair -> reconcile ซ้ำ เหมือนเดิม
คือซ่อมแบบตรวจแล้วซ่อม ไม่ใช่เขียนทับมั่ว ๆ ทุกรอบ

**ผลวัดจริง** (tests/test_sync_cost.py): ร้านที่มีประวัติ 2,000 บิล ซิงก์บิลใหม่ 5 ใบ
เดิมอ่าน 2,001 doc / เขียน 26 -> ตอนนี้อ่าน 6 / เขียน 1 และ**ต้นทุนไม่โตตามประวัติ**
(ร้าน 200 บิล กับ 2,000 บิล จ่ายเท่ากัน)

**ของใหม่:** `fake_firestore` นับ read/write ได้แล้ว (`db.reads`, `db.writes`,
`db.reset_meter()`) — บั๊กสองตัวนี้ไม่มีทางจับได้ด้วยเทสต์ที่ถามว่า "คำตอบถูกไหม"
ต้องมีเทสต์ที่ถามว่า "จ่ายไปเท่าไหร่"

---

## 7.2 ยอดคงเหลือแบบ snapshot + ตัดสต๊อกเป็นชุด (ทำแล้ว - A3/A5)

**ปัญหา:** ยอดคงเหลือคือ "ผลรวมของ movement ทั้งหมด" และทุกบิลที่ขายเขียน
movement 1 แถวต่อวัตถุดิบ 1 ตัว ร้าน 100 บิล/วัน เมนู 3 วัตถุดิบ = ~110,000 movement/ปี
หน้า "ของในครัว" (หน้าที่พนักงานเปิดบ่อยสุด) อ่านทั้งหมดนั้นทุกครั้งที่เปิด
แถมยังยิง query แยกต่อวัตถุดิบอีก 1 ครั้งเพื่อหาต้นทุนเฉลี่ย

**แก้:** เก็บยอดสะสมไว้บน material doc — `stock_qty`, `recv_qty`, `recv_value`
อัปเดตด้วย `Increment` ของ Firestore **ใน batch เดียวกับ movement** ที่ทำให้มันเปลี่ยน

- batch ของ Firestore เป็น atomic -> snapshot กับ ledger ไม่มีทางไม่ตรงกัน
  ถ้าพังกลางคันคือไม่เกิดทั้งคู่ ไม่ใช่เกิดอย่างเดียว
- `Increment` ไม่ใช่ read-modify-write -> ขาย 2 บิลพร้อมกันไม่ทับกันเอง
- ต้นทุนเฉลี่ยถ่วงน้ำหนักไม่ additive แต่ **สองครึ่งของมัน additive**
  เก็บ `recv_qty` กับ `recv_value` แล้วหารตอนอ่าน ได้ค่าเดิมเป๊ะไม่ว่าของเข้าลำดับไหน

**snapshot เป็นค่าที่คำนวณได้ ไม่ใช่ความจริง** — ledger ยังเป็นตัวจริงเสมอ
`rebuild_snapshots()` สร้างใหม่จาก ledger ได้ตลอด (ปุ่ม "คำนวณยอดคงเหลือใหม่" ในหน้าตั้งค่า)
วัตถุดิบที่ยังไม่มี snapshot (ของเก่าก่อนอัปเดตนี้) จะ **ตกไปใช้ทางเดิมคืออ่าน ledger ทั้งหมด**
ช้าแต่ไม่ผิด — ยอมช้า ดีกว่าตอบเลขสต๊อกผิดแบบมั่นใจ

**A5 ตัดสต๊อกเป็นชุด:**
- `store.all_recipes()` อ่านครั้งเดียวต่อ sync แทนที่จะอ่านสูตรต่อรายการต่อบิล
- movement ทั้งหมดเขียนแบบ batch (200 movement/batch เพราะ 1 movement = 2 operation)
- ปิดรอบนับ: ใช้ id ที่ `record()` คืนมา แทนที่จะไล่อ่าน movement ทั้งหมดของวัตถุดิบ
  แล้วเดาว่า "อันใหม่สุดคืออันที่เพิ่งเขียน" — ซึ่งเป็นการเดา ไม่ใช่ข้อเท็จจริง
  ถ้ามีสองอันเขียนในวินาทีเดียวกัน
- สูตรที่อ้างวัตถุดิบที่ถูกลบไปแล้ว: ข้ามแล้ว**รายงานชื่อออกมา** (`unknown_materials`)
  แทนที่จะหายเงียบ ๆ

**ผลวัดจริง** (tests/test_stock_snapshot.py): วัตถุดิบ 3 ตัว ที่มี movement 2,000 แถว
เดิมอ่าน 2,000+ doc -> ตอนนี้ **3 doc** และไม่โตตามประวัติ

**ข้อควรรู้ 2 ข้อ**
1. `rebuild` เขียนยอดที่อ่านมาเมื่อครู่ ถ้ามีบิลเข้ามาพอดีจังหวะนั้นจะโดนทับ
   -> กดซ้ำ หรือรอบนับถัดไปแก้เอง (เขียนเตือนไว้ในหน้าตั้งค่าแล้ว) เลยเป็นปุ่มไม่ใช่ของอัตโนมัติ
2. ทศนิยมสะสมนิดหน่อย (~1e-13 หลัง 2,000 ครั้ง) เพราะบวกสะสมแทนบวกใหม่ทุกครั้ง
   ไม่มีผลกับหน่วย กก./ขวด/ชิ้น และ rebuild ล้างให้สะอาดได้

**ยังไม่ได้ทำ:** `analyse_session` (หน้าส่วนต่าง) ยังอ่าน movement ทั้งร้าน
ควรเปลี่ยนเป็น query ช่วง `occurred_at` ระหว่างสองรอบนับ — แต่หน้านี้เปิดนาน ๆ ครั้ง เลยยังไม่เร่ง

---

## 7.3 หลายบัญชี Loyverse ต่อหนึ่งธุรกิจ + auto sync ไม่บล็อก API (ทำแล้ว - D1/A4)

**ปัญหาจากการใช้งานจริง:** แม่ค้าที่มีหลายสาขา เปิดบัญชี Loyverse แยกกันคนละสาขา
ไม่ได้เพิ่มสาขาเข้าบัญชีเดียว คือ **1 token = 1 สาขา**
ระบบเดิมสมมติว่า 1 ธุรกิจ = 1 บัญชี (`get_setting("loyverse_token")` คีย์เดียวต่อ tenant)
เธอจึงเชื่อมได้สาขาเดียว สาขาที่เหลือเข้าไม่ถึงเลย

**โครงใหม่**
```
tenants/{tid}/loyverse_connections/{conn_id}   { token, label, created_at, last_error }
tenants/{tid}/app_settings/store_index          { <store_id>: <conn_id> }
```

- **ไม่มี `c.provider` อีกแล้ว** — ลบทิ้ง ไม่เก็บเป็น fallback
  "provider" เฉย ๆ ไม่ใช่สิ่งที่มีอยู่จริงเมื่อธุรกิจถือหลายบัญชี
  ถามโดยไม่บอกว่าสาขาไหน = คำถามที่ไม่มีคำตอบถูก และของเดิมมันตอบให้ด้วยการ
  หยิบบัญชีแรกที่เจอ ทุก endpoint มี `{store_id}` ใน path อยู่แล้ว จึงบอกได้ทุกที่
  -> `c.provider_for(store_id)`
- ตรรกะการหาบัญชีอยู่ใน `core/pos_registry.py` ไม่ใช่ใน main.py จะได้เทสต์ได้
  โดยไม่ต้องมี fastapi (harness ออฟไลน์ไม่มี)
- `store_index` เป็น doc เล็ก ๆ ตัวเดียว -> ตอบ "สาขานี้ใช้ token ไหน" ด้วย read เดียว
  ถ้าเจอสาขาที่ไม่มีใน index (เพิ่งเปิดสาขาใหม่ใน Loyverse) จะ refresh เองอัตโนมัติ
  ไม่ใช่ error — เพราะไม่มีใครรู้ว่าต้องมากดอะไรหลังเปิดสาขาใหม่
- **บัญชีเดียวพังไม่ล้มทั้งระบบ** — token หมดอายุของบัญชีหนึ่ง จะรายงานบนบัญชีนั้น
  (`last_error`) สาขาของบัญชีอื่นยังใช้ได้ปกติ นี่คือเหตุผลที่แยกกันตั้งแต่แรก
- **ข้อมูลแยกขาดกัน** ไม่มีการรวมข้ามบัญชี แต่ละสาขาเก็บใต้ `stores/{store_id}` เหมือนเดิม
  store id จากคนละบัญชี Loyverse ไม่มีทางชนกัน
- **ของเดิมย้ายเอง** ธุรกิจที่มี `loyverse_token` เดิมจะกลายเป็น connection #1
  (ชื่อ "บัญชีหลัก") ครั้งแรกที่มีอะไรเรียกใช้ ไม่ต้องเชื่อมใหม่ ไม่ต้องบอกใคร
  (คีย์เดิมไม่ได้ลบ เผื่อ rollback)
- token ย้ายเข้า **body** แล้ว (`POST /api/settings/connections`)
  ของเดิม `POST /api/settings/token?token=` ยังอยู่ เผื่อ frontend เก่ายังไม่ได้ deploy

**UI:** หน้าตั้งค่ามีรายการบัญชี Loyverse (เพิ่ม/นำออก + เห็นสาขาของแต่ละบัญชี)
สลับสาขาได้จาก **หน้าแรก** แล้ว (เดิมต้องเข้าหน้าตั้งค่า ซึ่งเป็นหน้าสำหรับของที่ตั้งครั้งเดียว
แต่ "กำลังดูสาขาไหน" เจ้าของร้าน 3 สาขาเปลี่ยนวันละหลายรอบ)
ชื่อบัญชีจะโชว์ต่อท้ายชื่อสาขา **เฉพาะตอนที่มีมากกว่า 1 บัญชี** เพราะสองบัญชีอาจมี "สาขา 1" เหมือนกัน

**A4 — auto sync บล็อก API ทั้งเซิร์ฟเวอร์**
`sync_branch()` เป็น blocking I/O (HTTP + Firestore) ทั้งหมด แต่ถูกเรียกตรง ๆ ใน async task
ระหว่าง sync ทุก tenant ทุกสาขา **request อื่นไม่ได้แค่ช้า แต่หยุดสนิท**
ยิ่งมีลูกค้าเยอะยิ่งค้างนาน ซึ่งกลับหัวกลับหางจากที่ควรเป็น
-> `asyncio.to_thread()` + เปลี่ยน `on_event("startup")` (เลิกใช้แล้ว) เป็น `lifespan`

**ข้อจำกัดที่ต้องรู้:** loop นี้อยู่ในโปรเซส API ถ้ารันมากกว่า 1 instance ทุกตัวจะ sync ทุกอย่าง
การบันทึกเป็น idempotent แต่สอง instance อาจอ่าน "ยังไม่ประมวลผล" พร้อมกันแล้วตัดสต๊อกซ้ำ
**ก่อน scale เกิน 1 instance** ต้องย้าย sync ออกไปเป็น cron ยิง endpoint หรือจับ lock ใน Firestore
(เขียนเตือนไว้ใน docstring ของ `auto_sync_loop` แล้ว)

---

## 7.4 บั๊กที่มองไม่เห็นจนกว่าจะถึงวันนั้น + CI (ทำแล้ว - B/C)

### doc id ที่สร้างจากข้อความที่ผู้ใช้พิมพ์ (B7)

Firestore ห้าม doc id มี `/` ห้ามเป็น `.` `..` ห้ามขึ้นลงท้าย `__` และยาวไม่เกิน 1,500 ไบต์
แต่โค้ดเดิมใช้ **ชื่อเมนูเป็น doc id ตรง ๆ** ทั้ง `recipes`, `item_categories`,
`recipe_drafts`, `recipe_skips` — เมนูชื่อ **"ชา/กาแฟ"** บันทึกไม่ได้เลย
และ error ที่ได้มาจากลึก ๆ ใน SDK ไม่ได้พูดถึงเมนูหรือชื่อสักคำ

- `doc_key(name)` — ชื่อที่ Firestore รับได้อยู่แล้ว **ใช้เป็น id ตัวเองเหมือนเดิม**
  (ไม่ต้อง migrate ข้อมูลเก่าเลย และเปิด console ยังอ่านออกว่า "ผัดไท")
  ชื่อที่ใช้ไม่ได้ค่อย hash
- เก็บ `item_name` ไว้ **ข้างในเอกสาร** แล้วให้การอ่านคืนค่านั้น ไม่ใช่ id
  `all_recipes()` เปลี่ยนไป key ด้วยชื่อจริง เพราะรายงานยอดขายค้นด้วยชื่อเมนูที่ขาย

**เจอเพิ่มระหว่างทาง:** id ของวัตถุดิบสร้างจาก **ฝั่ง frontend** ด้วยการ slug ชื่อ 3 ที่
คนละสูตร — `Materials.jsx` ไม่มี timestamp ด้วย แปลว่า **วัตถุดิบสองตัวชื่อเดียวกัน
ทับกันเงียบ ๆ** และชื่อที่มี `/` สร้างไม่ได้เลย
-> รวมเป็น `util/ids.js` ตัวเดียว + backend ปฏิเสธ id ที่ใช้ไม่ได้พร้อมบอกว่า id ไหน

### read-modify-write ทับกันเอง (B8)

- **`set_count_entry`** อ่าน `entries` ทั้งก้อน แก้ 1 คีย์ แล้วเขียนกลับทั้งก้อน
  การนับสต๊อกคือ**งานเดียวในระบบที่สองคนทำพร้อมกันจริง ๆ** (คนหนึ่งของแห้ง คนหนึ่งตู้เย็น)
  คนที่เซฟทีหลังลบทุกอย่างที่อีกคนนับไปตั้งแต่ตอนที่ตัวเองอ่าน — ไม่ error ไม่มีสัญญาณ
  ตัวเลขหายเฉย ๆ และรอบนับดู "เสร็จแล้ว"
  -> เขียนทีละ field ด้วย `FieldPath("entries", material_id)`
  (ใช้ FieldPath ไม่ใช่ต่อ string ด้วยจุด เพราะ id เป็นข้อความจากผู้ใช้ ถ้ามีจุดจะกลายเป็นสองชั้น)
- **`add_alias`** อ่าน list เติม 1 ชื่อ เขียนกลับทั้ง list — ยืนยันใบส่งของหนึ่งใบ
  เรียน alias ทุกบรรทัดพร้อมกัน สองบรรทัดที่จบพร้อมกันจะทับกัน
  -> `ArrayUnion` / `ArrayRemove`

### อื่น ๆ

- **B6** ย้ายเฉพาะที่รั่วจริงเข้า body: **invite token** (= กุญแจเข้าธุรกิจ),
  อีเมลผู้ถูกเชิญ (PII), และ peek invite ที่มี token ใน path
  ที่เหลือ 19 endpoint เป็นข้อมูลธุรกิจธรรมดา (จำนวน ชื่อ หมวด) **ไม่แปลง**
  เพราะเหตุผลของ B6 คือความลับรั่วเข้า log ไม่ใช่ความสวยของ REST
  endpoint เก่ายังอยู่ทั้งหมด (มาร์ค deprecated) เผื่อ frontend ยังไม่ได้ deploy
- **B9** CORS อ่านจาก `ALLOWED_ORIGINS` ไม่ตั้ง = `*` เหมือนเดิม + print warning ตอน start
  (ความเสี่ยงจริงต่ำเพราะ auth เป็น Bearer token ไม่ใช่ cookie แต่แก้ทีเดียวจบ)
- **B10** route `/alerts` ประกาศซ้ำ 2 ที่ ลบตัวที่ 2 ทิ้ง (FastAPI ใช้ตัวแรก ตัวหลังเป็นโค้ดตาย
  ที่แก้ไปก็ไม่มีผล)

### C — CI

- `backend/tests/run_all.py` รันทุกไฟล์ในคำสั่งเดียว ตั้ง PYTHONPATH ให้เอง
  ข้ามตัวที่ต้องใช้ network/API key และถ้าพังจะพิมพ์ **บรรทัด [FAIL] จริง ๆ** ออกมา
  ไม่ใช่แค่ชื่อไฟล์
- `.github/workflows/tests.yml` — 2 job:
  - **backend**: `pip install -r requirements.txt` (พิสูจน์ว่าเวอร์ชันที่ pin ไว้ยังลงด้วยกันได้
    ซึ่งคือบั๊กในข้อ 8) แล้วรัน run_all.py
  - **frontend**: `npm ci && npm run build` — ยังไม่มีเทสต์ frontend
    แต่ build จับ syntax error / import ผิดในหน้าที่ไม่มีใครเปิดก่อน deploy ได้

---

## 7.5 แก้/ลบรายจ่ายได้ (ทำแล้ว)

**จากการใช้งานจริง:** บันทึกรายจ่ายผิดแล้วแก้ไม่ได้เลย backend มีแค่ `add_expense`
กับ `list_expenses` ไม่มีทั้ง update และ delete — พิมพ์ 3,400 เป็น 34,000
กำไรเดือนนั้นก็ผิดไปสามหมื่นตลอดกาล

**เพิ่ม:** `PUT` / `DELETE /api/{store_id}/expenses/{expense_id}` + ปุ่ม "แก้ไข" / "ลบ"
ในแต่ละแถวของหน้ารายรับรายจ่าย ใช้ฟอร์มเดียวกับตอนบันทึก (แก้ไขไม่ใช่งานคนละอย่าง
มันคืองานเดิมที่ทำใหม่ด้วยเลขที่ถูก)

**การตัดสินใจที่คุยกันไว้**
- **ลบแล้วหายเลย ไม่เหลือร่องรอย** — ต่างจากการนับสต๊อกที่เขียน movement ใหม่แทนการแก้เลข
  เพราะส่วนต่างของสต๊อก*คือ*ข้อมูลเกี่ยวกับครัว แต่รายจ่ายที่พิมพ์ผิดไม่ใช่ข้อมูลอะไรเลย
  มันคือ typo เก็บซากไว้มีแต่ทำให้อ่านยาก
- **แก้ได้ทุกเดือน** ไม่ใช่แค่เดือนปัจจุบัน (ปุ่ม "+ บันทึกรายจ่าย" ยังจำกัดเดือนปัจจุบันเหมือนเดิม
  กันการย้อนไปบันทึกงบเดือนเก่าโดยไม่ตั้งใจ — แต่เลขที่ผิดอยู่แล้วมันผิดจนกว่าจะมีคนแก้)

**จุดที่ต้องระวัง (มีเทสต์คุมแล้ว)**
- แก้แล้ว**เปลี่ยนหมวดได้** และแต่ละหมวดดึงด้วย query แยกกัน -> ต้อง refresh ทั้ง 3 หมวด
  ไม่งั้นจะเหลือสำเนาค้างในหมวดเดิม = ยอดรวมเบิ้ล
- `update_expense` **กรองเฉพาะ 4 ฟิลด์** ไม่ส่ง request body ทั้งก้อนลง Firestore
- กฎ validation ย้ายไป `core/expenses.py` (raise `ExpenseError` แล้ว main.py แปลงเป็น 400)
  เพื่อให้**บันทึกกับแก้ไขใช้กฎชุดเดียวกัน** — ถ้าแยกกัน การแก้ไขจะกลายเป็นช่องให้บันทึก
  รูปแบบที่ตอนบันทึกปฏิเสธ
- ปฏิเสธจำนวนเงิน **ติดลบ** ด้วย ไม่ใช่แค่ 0 — บางคนจะใช้ยอดติดลบ "หักล้าง" รายการเก่า
  ซึ่งทิ้งสองแถวค้างไว้ตลอดกาล วิธีที่ถูกคือลบแถวที่ผิด
- หมวด **ค่าวัตถุดิบ** ยังบันทึกใหม่ไม่ได้เหมือนเดิม (ระบบคำนวณเอง จะนับซ้ำ)
  แต่รายการเก่าที่อยู่ในหมวดนั้นแล้ว**แก้ได้** ไม่งั้นพอแก้ชื่อมันจะถูกย้ายหมวดเงียบ ๆ

---

## 7.6 ออเดอร์นอกร้าน — ขายผ่าน Grab/โทรสั่ง ให้ตัดสต๊อกได้ (ทำแล้ว เฟส 1)

**จากการใช้งานจริง:** ลูกค้าทดสอบขายผ่าน Grab แต่**จดใส่กระดาษ** ไม่ได้เข้า Loyverse
วัตถุดิบออกจากครัวโดยไม่มีอะไรบันทึก → ทุกออเดอร์ทำให้ variance ผิดขึ้นอีกนิด
โดยหน้าตาเหมือนเดิมทุกประการ ผ่านไปเดือนหนึ่ง "ของหาย 8 กิโล" ไม่มีความหมายเลย
เพราะแยกไม่ออกว่าของหายจริงหรือขาย Grab ไป 30 ออเดอร์

**ทำไมไม่ต่อ API Grab ตรง ๆ:** Grab / LINE MAN / ShopeeFood ไม่มี public API ให้สมัครเอง
เปิดผ่าน **POS partner ที่ได้รับอนุมัติ**เท่านั้น (LMWN ถึงขั้นซื้อ FoodStory มาเป็นของตัวเอง)
ตลาดเลยเต็มไปด้วย middleware อย่าง Deliverect/Klikit — เรื่องเดียวกับข้อ 3 (POSPOS/Ocha)

**เฟส 1 ที่ทำแล้ว:** หน้า "ออเดอร์นอกร้าน" บันทึกออเดอร์ด้วยมือ แทนกระดาษ
เลือกช่องทาง (Grab / LINE MAN / ShopeeFood / โทรสั่ง / เมนูออนไลน์ / รับหน้าร้าน)

**เฟส 2 (ยังไม่ทำ):** อ่านไฟล์รายงานจาก Grab — รอไฟล์จริงก่อน ไม่เขียนแบบเดาคอลัมน์
(บทเรียนเดียวกับ POSPOS ในข้อ 3) พอถึงตอนนั้นมันจะเป็นแค่ "อีกวิธีสร้างบิลชนิดเดิม"

### สิ่งที่ออกแบบไว้

- **บันทึกเป็นบิลขายธรรมดา** ลง collection `sales` เดิม รูปแบบเดิม
  → ทุกหน้ารายงานนับให้เองโดยไม่ต้องแก้อะไรเลย เพราะทุกหน้าอ่าน `list_sales`
- **ตัดสต๊อกผ่านสูตรเดียวกับขายหน้าร้าน** — `deductions_for()` แยกออกมาจาก `_apply`
  ให้ทั้ง sync และการบันทึกมือใช้ตัวเดียวกัน (ออเดอร์ Grab กับลูกค้าหน้าร้าน
  สั่งเมนูเดียวกันต้องกินวัตถุดิบเท่ากัน — สองเส้นทางคำนวณแยกกันคือสองที่ให้ผิด)
- **ฟิลด์ `source`** — บิลจาก POS เป็น `"loyverse"` แถวเก่าที่เขียนก่อนมีฟิลด์นี้ไม่มีค่า
  `source_of()` อ่านค่าว่างเป็น POS **ไม่ใช่ unknown** ไม่งั้นประวัติทั้งปีจะหลุดจากการตรวจ

### บั๊กที่ต้องแก้พร้อมกัน (สำคัญ)

`reconcile_sales` เทียบบิลใน Loyverse กับบิลที่เราเก็บ แล้วรายงานตัวที่ขาด
พอมีบิลเดลิเวอรี่ มันจะขึ้นว่า "หายจาก POS" **ทุกใบตลอดกาล** และปุ่ม "อัปเดตข้อมูล"
หน้าแรกจะพยายามซ่อมทุกครั้งที่กด → กรองด้วย `is_pos_sale()` แล้ว

### การลบออเดอร์ที่บันทึกผิด

- `MovementLedger.delete_by_ref()` — **ลบ movement ทิ้งจริง** ไม่ใช่เขียน movement กลับทาง
  เพราะ variance นับ `abs(quantity)` ของ kind `sale` ถ้าเขียนกลับทาง usage จะกลายเป็น 2 เท่า
  (ขายออก 2 + คืน 2 = ใช้ไป 4) — เป็นข้อยกเว้นเดียวของกฎ "ledger ไม่ลบ"
  เหตุผลเดียวกับรายจ่าย: ออเดอร์ที่พิมพ์ผิดไม่ใช่ข้อมูลเกี่ยวกับครัว มันคือ typo
- **ห้ามลบถ้ามีการนับสต๊อกหลังออเดอร์นั้นแล้ว** — รอบนับเขียน delta เพื่อให้ยอดไปจบที่
  เลขที่นับได้จริง ซึ่งรวมวัตถุดิบก้อนนี้ไปแล้ว ถอนทีหลังยอดจะเพี้ยนจากเลขที่**คนนับมากับมือ**
- **ลบบิลจาก POS ไม่ได้** (404) — ลบที่นี่ไม่ได้ลบใน Loyverse รอบ sync ถัดไปมันกลับมา
  พร้อมตัดสต๊อกซ้ำอีกรอบ

### อื่น ๆ

- **`order_id` สร้างจากฝั่ง browser** ไม่ใช่ server → retry ปลอดภัย ยิงซ้ำได้ id เดิม
  ตัวที่สองโดนปฏิเสธ (409) แทนที่จะตัดสต๊อกซ้ำ
- **normalize เวลาเป็นรูปแบบเดียวกับบิล Loyverse** ก่อนบันทึก — `list_sales` เทียบ string
  ถ้าเก็บเป็น `+07:00` จะหลุดจากทุกช่วงวันที่ที่ควรมีมัน
- **ราคาที่บันทึกคือราคาที่ลูกค้าจ่ายบนแอป** ไม่ใช่ราคาหน้าร้าน
  ค่าคอมฯ Grab (30-35%) ลงเป็น**รายจ่ายรายเดือน**ตามใบแจ้งหนี้ ใช้ฟีเจอร์ในข้อ 7.5
  — ตรงกับความจริงและไม่ต้องเดาเปอร์เซ็นต์รายบิล
- เมนูที่ยังไม่มีสูตรจะบันทึกเงินได้แต่ไม่ตัดสต๊อก → **บอกออกมาตรง ๆ** หลังบันทึก
  ไม่ปล่อยให้ดูเหมือนบั๊ก
- สิทธิ์: ใช้ `view_money` ทั้งสาม endpoint (เจ้าของ + ผู้จัดการ) เหมือนรายจ่าย
  ถ้าวันหนึ่งพนักงานต้องรับออเดอร์เอง ค่อยเพิ่ม capability `record_sales` แยก

---

## 7.7 ค่าวัตถุดิบไม่เคยถูกนับเป็นรายจ่ายเลย (แก้แล้ว)

**เจอจากคำถามของเจ้าของ:** "ซื้อของเข้าร้านถูกคิดเป็นรายจ่ายค่าวัตถุดิบไหม"
คำตอบคือ **ไม่** — และไม่มีที่ไหนในระบบเขียนรายจ่ายหมวด `material` เลยสักจุด

`add_receiving()` บันทึกเอกสารรับของ + movement เข้าสต๊อก แต่ไม่สร้างรายจ่าย
ส่วนฟอร์มบันทึกรายจ่ายก็**จงใจซ่อนหมวดค่าวัตถุดิบ** พร้อมคอมเมนต์ว่า "ระบบคำนวณเองแล้ว"
— แต่ไม่มีใครคำนวณให้ ผลคือ:

| | ค่าที่เห็นก่อนแก้ |
|---|---|
| การ์ด "ค่าวัตถุดิบ" | **฿0 ตลอด** |
| "ค่าใช้จ่ายรวม" | ขาดค่าวัตถุดิบทั้งก้อน |
| **"กำไรสุทธิ"** | **สูงเกินจริง 30-40% ของยอดขาย** |
| "ซื้อจริง" ในการ์ดเทียบ | ฿0 → ส่วนต่างติดลบมหาศาล ดูเหมือนระบบพัง |

(หน้าแรกไม่พัง — `gross_profit` ใน `sales_report.py` คำนวณจากสูตรถูกอยู่แล้ว คนละเส้นทาง)

### concept 2 ส่วน ที่วางไว้แต่แรก (อยู่ในโค้ดครบแล้ว แค่ต่อสายผิด)

1. **ค่าวัตถุดิบ = ผลรวมใบรับของ (ซื้อจริง)** → เข้า "ค่าใช้จ่ายรวม" และ "กำไรสุทธิ"
   **เจ้าของเลือกเอง** เหตุผล: เจ้าของร้านอ่านกำไรสุทธิเทียบกับเงินในบัญชี
   ตัวเลขที่ไม่ตรงกับเงินจริงคือตัวเลขที่เขาเลิกเชื่อ
   ข้อแลก: มาเป็นก้อน ๆ เดือนที่ตุนของกำไรดูน้อย เดือนถัดมาดูเยอะ
2. **ตามสูตร × ยอดขาย** → อยู่ในการ์ดเทียบด้านล่าง นิ่งกว่าและเทียบเดือนต่อเดือนได้
   ส่วนต่างระหว่างสองตัวคือของที่ตุนเพิ่ม/ที่กินจากสต๊อกเดิม
3. **มูลค่าสต๊อกคงเหลือ** → แถวที่สี่ ใต้เส้นคั่น **ไม่ใช่ตัวเทียบตัวที่สาม**
   สมการ `ต้นงวด + ซื้อ − ใช้ = ปลายงวด` ปิดอยู่แล้ว และ ledger คำนวณสต๊อกจากสมการนั้นเป๊ะ
   เอามาเทียบกันจะได้ศูนย์เสมอ (การเทียบ "ควรเหลือ vs เหลือจริง" อยู่ที่หน้าส่วนต่างจากรอบนับ
   ซึ่งเทียบกับของที่คนนับมากับมือ ไม่ใช่กับตัวเลขที่ระบบคำนวณเอง)
   มันคือ**ยอดคงเหลือที่ส่วนต่างไหลไปกอง** — ตอบคำถาม "กำไรน้อยลง เงินหายไปไหน"
   ว่าเงินไปกองอยู่บนชั้น
   **ติดป้าย "ณ วันนี้"** เพราะ ledger เก็บยอดปัจจุบัน ไม่ได้เก็บรายวันย้อนหลัง
   เลือกดูเดือนก่อน = ซื้อ/ใช้ ของเดือนนั้น เทียบกับสต๊อกวันนี้ ถ้าไม่ติดป้ายจะหลอกตาแรง
   (จะทำให้ถูกย้อนหลังต้อง replay ledger ถึงวันสิ้นเดือน แพงและเป็นงานคนละขนาด)
   สต๊อกติดลบถูก clamp เป็น 0 — ติดลบแปลว่า ledger ผิดที่ไหนสักแห่ง ไม่ใช่ร้านเป็นเจ้าหนี้
   ปล่อยให้ลบจะไปหักล้างของจริงที่อยู่ตัวอื่นเงียบ ๆ

**ส่วนต่างไม่ใช่ error** — ซื้อมากกว่าใช้ = ตุนของ, ซื้อน้อยกว่าใช้ = กินของเก่า
เขียนอธิบายไว้ใต้การ์ดแล้ว

### ปัญหาที่ใหญ่กว่า ซึ่งเจอตอนขุด

หน้านี้ดึงยอดขายจาก **`api.getReceipts()` = อ่านสดจาก Loyverse** ไม่ใช่สำเนาที่เราเก็บ

- **ดูเดือนก่อนไม่ได้** แผนฟรีให้ 31 วัน ทั้งที่เราเก็บสำเนาไว้ครบ (เหตุผลทั้งหมดที่ทำระบบเก็บสำเนา)
- **ออเดอร์นอกร้านไม่โผล่เลย** (ข้อ 7.6) → รายรับและต้นทุนขาดหายทั้งก้อน
- ยิง `getRecipe` ทีละเมนูในเบราว์เซอร์ (N+1) ทั้งที่ backend คำนวณให้ใน read เดียว

**แก้:** ใช้ `/api/{store_id}/sales/overview` ซึ่งคืน `total`, `ingredient_cost`,
`gross_profit`, `uncosted_menus` มาให้ครบอยู่แล้ว — แก้ทั้งสามข้อพร้อมกัน
**แก้ frontend อย่างเดียว ไม่แตะ backend ไม่ต้อง migrate**

### อื่น ๆ

- เพิ่มคำเตือน **`uncosted_menus`** — เมนูที่ไม่มีสูตรนับเป็นรายรับแต่ไม่มีต้นทุน
  กำไรจะดูดีกว่าจริงโดยดูจากตัวเลขเองไม่มีทางรู้
- รายการค่าวัตถุดิบเก่าที่เคยกรอกมือไว้ **ยังแสดงในลิสต์แต่ไม่ถูกนับ** (ติดป้ายบอก + ลบได้)
  ถ้านับด้วยจะเป็นการคิดค่าอาหารก้อนเดียวกันสองรอบ

**ผลกระทบตอน deploy: กำไรสุทธิของลูกค้าจะลดลง** เพราะเดิมไม่ได้หักค่าวัตถุดิบเลย
ตัวเลขใหม่คือตัวเลขจริง — ควรบอกลูกค้าก่อนเขาเปิดมาเจอเอง

---

## 7.8 แก้/ลบรายการซื้อของเข้าร้านได้ (ทำแล้ว)

**ปัญหาเดียวกับรายจ่ายในข้อ 7.5 แต่หนักกว่า** — ใบรับของขยับ 2 อย่างพร้อมกัน:
สต๊อกขึ้นชั้น และราคาที่จ่ายเข้าไปอยู่ในต้นทุนเฉลี่ยซึ่งเป็นฐานของทุกตัวเลขกำไร
พิมพ์ 5 กก. เป็น 50 กก. จะดูเหมือนเดือนที่ดี จนกว่าจะมีคนไปนับชั้นวาง
และก่อนหน้านี้**แก้ไม่ได้เลย บันทึกแล้วจบ**

**เพิ่ม:** `PUT` / `DELETE /api/{store_id}/receivings/{id}` + ปุ่มแก้ไข/ลบในแต่ละแถว
ใช้ฟอร์มเดียวกับตอนบันทึก

### หลักที่ยึด

- **แก้เอกสารต้องแก้ ledger ด้วยเสมอ** — ใบรับของ*คือ* movement ของมัน
  ในสายตาของสต๊อกและต้นทุน ถ้าแก้กระดาษเป็น 50 แต่ ledger ยังเป็น 5
  จะมีสองคำตอบต่อคำถามเดียว และรายงานทุกหน้าอ่านตัวที่ผิด
  -> วิธีแก้คือ **ถอน movement เก่าออก แล้วเขียนชุดใหม่** (ไม่ใช่แก้ฟิลด์)
- **ต้นทุนเฉลี่ยต้องตามไปด้วย** — `delete_by_ref()` ถอน `recv_qty`/`recv_value`
  ของ movement นั้นออกจาก snapshot ให้ครบ ไม่งั้นราคาที่พิมพ์ผิดจะลากต้นทุนเฉลี่ย
  ไปตลอดกาลแม้แก้กระดาษแล้ว (มีเทสต์คุม: พิมพ์ 900 เป็น 220 แล้วเฉลี่ยต้องกลับมาถูก)
- **ห้ามแก้/ลบถ้ามีการนับสต๊อกหลังจากนั้นแล้ว** — ใช้ `_refuse_if_counted_since()`
  ตัวเดียวกับออเดอร์นอกร้าน รอบนับคือ**ตัวเลขเดียวในระบบที่วัดมาจริง ไม่ได้คำนวณ**
  ถอนของออกจากใต้มันคือการล้มคำตอบของคนที่ไปนับมากับมือ
- กฎ validation อยู่ใน `core/receiving.py` ใช้ร่วมกันทั้งบันทึกและแก้ไข
  (`add_receiving` เดิม**ไม่มี validation เลย**)
- **ราคาต่อหน่วย 0 ได้ แต่ติดลบไม่ได้** — ของแถม/ของตัวอย่างมาฟรีแต่ขึ้นชั้นจริง
  ส่วนใบที่ร้านได้เงินคืนคือการคืนของ ไม่ใช่ใบรับของติดลบ ซึ่งจะลากต้นทุนเฉลี่ย
  ต่ำกว่าที่ใครเคยจ่าย

---

## 7.9 หน้ารายรับรายจ่ายช้า — จ่ายค่าอ่านเป็นสองเท่าโดยไม่ได้อะไร (แก้แล้ว)

**เจ้าของสังเกตเองว่าโหลดช้าผิดปกติ** ไล่ดูแล้วร้าน 100 บิล/วัน เลือก "เดือนนี้"
อ่าน **~6,000 doc ต่อการเปิดหน้าหนึ่งครั้ง** เลือก "ทั้งปี" = ~72,000 doc

| อ่านอะไร | กี่ doc |
|---|---|
| ยอดขายเดือนนี้ | ~3,000 |
| **ยอดขายเดือนก่อน** เพื่อคำนวณ `compare` | **~3,000** |
| ใบรับของ **ทั้งหมดตั้งแต่เปิดร้าน** | ไม่จำกัด |

### 1. ช่วงเปรียบเทียบที่ไม่มีใครดู

`/sales/overview` คำนวณ `compare` ทุกครั้ง = อ่านช่วงเวลาที่สองขนาดเท่ากัน
แต่ **มีแค่หน้าแรกที่แสดง** หน้ารายรับรายจ่ายกับหน้ารายการขายไม่ได้แตะเลย
-> เพิ่มพารามิเตอร์ `compare` (ดีฟอลต์ `true` เพื่อไม่ให้ของเดิมพัง)
สองหน้านั้นส่ง `false` -> **อ่านลดครึ่งหนึ่งทั้งคู่**

### 2. `list_receivings()` อ่านทั้ง collection เสมอ

กับดักเดียวกับ `list_sales` ก่อนแก้ในข้อ 7.1 — ยิ่งใช้นานยิ่งช้า ทั้งที่คำถามเท่าเดิม
-> ใส่ตัวกรองช่วงวันที่แบบ push down ไป Firestore
(เทสต์คุม: เดือน ก.ค. 28 ใบ + ส.ค. 3 ใบ ถาม ส.ค. ต้องอ่าน **3 doc**)

**ต้องทำ `normalize_date()` ก่อน** เพราะ Firestore เทียบ range เป็น string
`"2026-08-15"` กับ `"2026-08-15T00:00:00Z"` คือวันเดียวกันที่เรียงคนละที่
ใบที่เขียนแบบหลังจะ**หลุดจากทุกเดือนที่ควรมีมัน พร้อมพาต้นทุนหายไปด้วย** = กำไรดูดีเกินจริง
- วันที่อ่านไม่ออกจะ**เก็บไว้ตามเดิม ไม่ทิ้ง** — ข้อมูลที่อ่านไม่ออกยังเป็นข้อมูลของร้าน
- `confirm_draft` (ใบจากสแกน AI) เดิม**ไม่ผ่าน validation เลย** ตอนนี้ผ่านชุดเดียวกับฟอร์ม
  และถ้าสแกนไม่เจอวันที่จะใช้**วันนี้**แทนการเก็บค่าว่าง ซึ่งเรียงอยู่ล่างสุดและไม่อยู่ในเดือนไหนเลย

### 3. บั๊กเวลาที่เจอระหว่างทาง

`from.slice(0, 10)` เพื่อเอาวันที่จาก ISO instant — **ได้วันผิดไปหนึ่งวัน**
เที่ยงคืนวันที่ 1 ส.ค. ตามเวลาไทย = 31 ก.ค. 17:00 UTC ตัดมาได้ `"2026-07-31"`
-> สร้างขอบเขตแบบวันที่ตรง ๆ จาก year/month ไม่ derive จาก instant

และเปลี่ยน `inPeriod()` มา**เทียบ string วันที่** แทนการ `new Date()` แล้วถาม `getMonth()`
— วันที่แบบ `YYYY-MM-DD` ถูกอ่านเป็นเที่ยงคืน UTC แล้วเอาไปถามคำถามเวลาท้องถิ่น
ซึ่งตกเดือนผิดที่ปลายทั้งสองข้างแล้วแต่ timezone (ไทย +7 รอดมาโดยบังเอิญ)

**ผลรวม:** เดือนนี้เหลือ ~3,000 doc จาก 6,000+ และใบรับของไม่โตตามอายุร้านอีก

**ยังเหลือ ~3,000 doc** ทางแก้จริงคือ `sales_daily/{date}` ในข้อ 7 (ดูเดือนนี้ = 30 doc)
ข้อแลก: แก้สูตรทีหลังแล้วกำไรย้อนหลังไม่อัปเดตเอง ต้องมีปุ่มคำนวณใหม่ — ยังไม่ได้ทำ

**หมายเหตุ:** พารามิเตอร์ `compare` ทดสอบแบบออฟไลน์ไม่ได้ (endpoint ต้องมี fastapi + auth)
ยืนยันได้แค่ว่าแอปบูตขึ้นและ route รับพารามิเตอร์นี้จริง — ตรรกะข้างในอ่านจากโค้ด

---

## 7.10 แคชคำตอบฝั่ง server (ยังไม่ทำ - ออกแบบไว้แล้ว)

**ที่มา:** หลังแก้ข้อ 7.9 หน้ารายรับรายจ่ายเหลือ ~3,000 doc ต่อครั้ง ยังไม่ช้ามาก
แต่ทุกครั้งที่เปลี่ยนหน้าไป-กลับก็จ่ายค่าอ่านใหม่ทั้งหมด ทั้งที่ข้อมูลไม่ได้เปลี่ยน
**เจ้าของบอกให้บันทึกไว้ก่อน ยังไม่ต้องทำ**

### กับดัก: แคชแบบ TTL อย่างเดียวจะรู้สึกเหมือนระบบพัง

แคช 300 วิเฉย ๆ แล้วเจ้าของบันทึกออเดอร์ Grab -> เปิดหน้ารายการขาย -> **ยังไม่ขึ้น**
-> กด refresh -> ยังไม่ขึ้น จากมุมคนใช้ นี่คือ "ระบบไม่บันทึกให้" ไม่ใช่ "แคชยังไม่หมดอายุ"
เขาจะกดบันทึกซ้ำ แล้วได้ออเดอร์สองใบ

### แบบที่ออกแบบไว้ — แคช + ล้างทันทีเมื่อมีการเขียน

```
_cache[(tenant_id, store_id)] = { version, entries: {key: (expires, payload)} }
```

- **ล้างทันทีที่เขียน** — บันทึกออเดอร์ / แก้รายจ่าย / รับของ / ปิดรอบนับ / sync จบ
  -> เด้ง `version` -> entries เดิมเข้าไม่ถึงอีก (ไม่ต้องไล่ลบทีละคีย์)
- **TTL = ค่า sync interval ของร้านนั้น** (`_sync_interval()` มีอยู่แล้ว) เป็นตาข่ายรอง
  สำหรับอะไรที่เปลี่ยนโดยเราไม่รู้
- แคช**คำตอบ** ไม่ใช่เอกสารดิบ -> กินแรมนิดเดียว
- ที่จะแคช: `/sales/overview`, `/sales/daily`, `/receivings`, `/expenses`, `/alerts`

ผลคือบันทึกอะไรแล้วเห็นทันที แต่เปลี่ยนหน้าไป-กลับเฉย ๆ ไม่เสียค่าอ่าน

### ⚠️ จุดที่พลาดไม่ได้

**คีย์ต้องมี `tenant_id` เสมอ** — พลาดตรงนี้ร้าน A จะเห็นยอดขายร้าน B
ซึ่งเป็นความเสียหายคนละระดับกับเรื่องความเร็วทั้งหมด

ให้ฟังก์ชันสร้างคีย์ **บังคับ**รับ tenant_id ไม่ใช่ให้คนเรียกจำเอง — หลักเดียวกับ
`Store.for_tenant()` ที่ทำให้ tenant isolation เป็นเรื่องเชิงโครงสร้าง ไม่ใช่กฎที่ต้องจำ
และต้องมีเทสต์เฉพาะ: สองร้านถามคำถามเดียวกัน ต้องได้คนละคำตอบ

### ข้อจำกัด

แคชอยู่ในโปรเซส **เหมือน `auto_sync_loop`** — รันเกิน 1 instance เมื่อไหร่ แต่ละตัวมีแคชของตัวเอง
เขียนที่ instance A อ่านจาก B จะเห็นของเก่าจน TTL หมด
เข้าคิวอยู่ในรายการเดียวกับ `auto_sync_loop` ที่ต้องแก้ก่อน scale

### ทางเลือกอื่นที่ยังไม่ได้ตัดสินใจ

ถ้ายอมแลกความสดของกำไรย้อนหลัง **`sales_daily/{date}`** (ข้อ 7) แก้ที่ต้นเหตุกว่า
— ดูเดือนนี้อ่าน 30 doc แทน 3,000 แล้วแคชอาจไม่จำเป็นเลย
ข้อแลก: แก้สูตรทีหลังแล้วกำไรย้อนหลังไม่อัปเดตเอง ต้องมีปุ่มคำนวณใหม่

**อัปเดต:** ทำแล้วในข้อ 7.11 — และ**ข้อแลกที่กลัวไว้ไม่เกิดขึ้น** เพราะเก็บแต่ของที่
เปลี่ยนไม่ได้ ไม่เก็บต้นทุน (อ่านรายละเอียดข้อ 7.11) แคชฝั่ง server จึงยังไม่จำเป็น

---

## 7.11 sales_daily — สรุปวันละ 1 doc (ทำแล้ว — เฟส 0 ของผู้ช่วย AI)

**ที่มา:** เดือนหนึ่งมีบิล ~3,000 ใบ ทุกหน้าที่แสดง "เดือนนี้" อ่านครบทั้ง 3,000 ใบ
ทั้งที่คำตอบมีขนาดเท่าเดิมไม่ว่าร้านเปิดมากี่ปี และเป็นของที่ต้องมีก่อนทำผู้ช่วย AI
(สรุปประจำวัน / ตอบคำถามย้อนหลัง จะให้ AI อ่านบิลดิบเป็นพันใบไม่ได้)

### กุญแจ: เก็บเฉพาะสิ่งที่เปลี่ยนไม่ได้

หนึ่งวัน = หนึ่ง doc เก็บแค่ **ข้อเท็จจริง**

```
{date, total, bill_count, refund_count, by_source:{grab:{total,count}}, items:{ชื่อ:{qty,revenue}}}
```

**ไม่เก็บต้นทุนวัตถุดิบ ไม่เก็บกำไร** — ยอดขายและจำนวนจานที่ขายเป็นข้อเท็จจริง
จริงตอนกดบิลและจริงตลอดไป แต่ต้นทุนคือจำนวนจาน × สูตร × ราคาวัตถุดิบ
ซึ่งสองอย่างหลังถูกแก้ย้อนหลังตลอด ถ้าเก็บไว้คือ "แช่ค่าที่เดาไว้"
คำนวณตอนอ่านจาก 30 แถวแทบไม่มีต้นทุน และ**แก้ของที่ผิดวันนี้ กำไรเดือนที่แล้ว
ขยับทันทีโดยไม่ต้องกดคำนวณใหม่** — ปุ่ม rebuild ที่ข้อ 7 กลัวว่าต้องมี จึงไม่ต้องมี

ข้อแลกของทางนี้ (ราคาที่ขึ้นจริงก็ลามไปอดีตด้วย) อยู่ในหัวข้อ ⚠️ ท้ายข้อนี้
— **อ่านก่อนคิดจะเปลี่ยน**

### กฎสองข้อที่เหลือ

- **ไม่เก็บ "วันนี้" เด็ดขาด** — บิลยังทยอยเข้า แถวที่เขียนตอนบ่ายสองจะถูกอ่าน
  ตอนปิดร้านเป็นยอดทั้งวัน ผิดในทางที่ไม่มีใครเช็ค คือผิดแบบน้อยกว่าจริง
  วันนี้คำนวณจากบิลสดทุกครั้ง (= บิลวันเดียว ซึ่งคือราคาที่เมื่อก่อนจ่ายเพื่อดูทั้งเดือน)
- **วันที่ร้านปิดเก็บเป็นศูนย์** ไม่ใช่ปล่อยว่าง ไม่งั้นวันอาทิตย์ที่ปิดร้าน
  หน้าตาเหมือนวันที่ยังไม่เคยสรุป แล้วจะกลับไปอ่านบิลใหม่ทุกครั้งตลอดไป

### ล้างเมื่อของเปลี่ยน — ลบทิ้ง ไม่ใช่แก้ทีละช่อง

บิลมาสาย / ออเดอร์นอกร้านเพิ่มหรือลบ / repair -> `delete_daily(วันนั้น)`
รอบหน้าที่มีคนเปิดดู มันสร้างใหม่จากบิลเอง แถวที่หายไปผิดไม่ได้ แถวที่แก้ทีละช่องผิดได้เงียบ ๆ

จุดที่ล้าง: ใน `_apply()` ของ stock_engine (ครอบ sync + repair ทีเดียว)
และที่ endpoint เพิ่ม/ลบออเดอร์นอกร้าน

### ⚠️ ของที่ต้องทำก่อน: ร้านไม่เคยเก็บ timezone

เดิม browser ส่ง `tz_offset` มากับทุก request — ใช้ได้กับหน้าจอที่มีคนดูอยู่
แต่ใช้ไม่ได้เลยกับอะไรที่ server ทำเอง (สรุปประจำวัน / rollup) เพราะไม่มี browser ให้ถาม

-> เก็บ `timezone_offset` ใน settings ของร้าน, frontend ส่งให้ครั้งเดียวตอน login แรก
(`-new Date().getTimezoneOffset()`), **คำตอบแรกชนะ** — เจ้าของไปเที่ยวต่างประเทศ
แล้วเปิดแอป ต้องไม่ทำให้เส้นแบ่งวันของร้านย้ายทั้งประวัติ

### บั๊กที่เจอระหว่างทาง

`daily_breakdown()` แบ่งวันตาม **UTC** ขณะที่กราฟข้าง ๆ แบ่งตามเวลาร้าน
บิลตอนสองทุ่มจึงอยู่คนละวันระหว่างรายการกับกราฟ **ในหน้าจอเดียวกัน**
rollup แก้ให้เองเพราะทั้งสองอ่านจากแถวเดียวกันแล้ว

### วัดผล

| | ก่อน | หลัง |
|---|---|---|
| เปิดดูเดือนที่มี 3,000 บิล | ~3,000 doc | ~30 doc |
| sync ปกติ (บิลของวันนี้) | - | ไม่มี write เพิ่มเลย |

เทสต์: `tests/test_daily_rollup.py` (84 checks) — เทียบ rollup กับ `sales_report.summarise()`
ให้ตรงกันเป๊ะ, แก้ราคาวัตถุดิบแล้วกำไรย้อนหลังขยับเอง, บิลมาสายล้างวันเก่า,
sync ของวันนี้ไม่ล้างอะไร, และ**วัดค่าอ่าน** เดือนละ ≤ 35 doc

### ⚠️ ต้นทุนตามสูตรคิดที่ราคาวัตถุดิบ**ล่าสุด** — ตั้งใจ ไม่ใช่บั๊ก

**เจ้าของถามตรง ๆ ว่า "ปรับราคาวัตถุดิบเดือนนี้ แล้วกำไรเดือนที่แล้วลดตามด้วยเหรอ"** — ใช่

พฤติกรรมนี้**มีมาก่อน rollup แล้ว** (`summarise()` เดิมก็คิดบิลเก่าด้วยราคาวันนี้)
rollup แค่ทำเหมือนเดิมเป๊ะ ๆ ซึ่งเทสต์ที่เทียบสองทางคือตัวยืนยัน

มันคือของที่**เลือกได้อย่างเดียว** ไม่มีทางได้ทั้งคู่:

| | คิดตอนอ่าน (ตอนนี้) | บันทึกตอนขาย |
|---|---|---|
| พิมพ์ราคาผิดแล้วมาแก้ | อดีตตามให้เอง ✅ | อดีตค้าง ต้องมีปุ่มคำนวณใหม่ |
| ราคาขึ้นจริง | ลามไปอดีตด้วย ❌ | อดีตอยู่นิ่ง ✅ |
| เมนูที่เพิ่งผูกสูตรทีหลัง | คิดต้นทุนย้อนหลังให้ ✅ | ต้นทุน 0 ตลอดไป ❌ |

**เลือกอยู่ทางเดิม** เพราะ:
1. **กำไรสุทธิไม่ได้ใช้ตัวเลขนี้** — ใช้ยอดซื้อจริง (ข้อ 7.7) ตัวที่เทียบกับเงินในบัญชีปลอดภัย
   ที่กระทบคือ `gross_profit` หน้าแรก กับช่อง "ตามสูตร" ในการ์ดเทียบเท่านั้น
2. **เพี้ยนน้อยและช้า** — `cost` = `recv_value / recv_qty` เป็นค่าเฉลี่ย**ตลอดอายุร้าน**
   ซื้อแพงขึ้นหนึ่งเดือนขยับแค่ไม่กี่ % เล็กกว่าของที่การ์ดนี้มีหน้าที่จับ (ของเสีย ตักเกินสูตร)
3. **ร้านยังผูกสูตรไม่ครบ** — แถวที่สามในตารางข้างบนตอนนี้มีค่ามากกว่าแถวที่สอง

**ทำแทน:** เปลี่ยนป้ายเป็น `ตามสูตร (ราคาวัตถุดิบล่าสุด)` — คนอ่านรู้ว่าตัวเลขแปลว่าอะไร ไม่ต้องเดา

**ถ้าวันหนึ่งต้องแก้จริง** (ราคาเหวี่ยงแรงจนตัวเลขไม่น่าเชื่อ): ช่อง `unit_cost`
**มีอยู่แล้วใน movement** แต่ `deductions_for()` ปล่อยว่างตอนตัดจากการขาย
เติมค่าตรงนั้น + ให้ rollup เก็บ `ingredient_cost` เป็นข้อเท็จจริง = จบ
รอให้มีของจริงให้ดูก่อนว่าเพี้ยนเท่าไหร่ ดีกว่าเดาตอนนี้

---

## 7.12 ผู้ช่วย AI — เฟส 1: กำแพงสองด้านรอบโมเดล (ทำแล้ว)

**โจทย์ที่แท้จริงไม่ใช่ "ให้ AI ตอบเก่ง"** — เจ้าของร้านเอาคำตอบไปเทียบกับอะไรไม่ได้เลย
นั่นคือเหตุผลที่เขาถาม ผู้ช่วยที่ถูก 9 ใน 10 ครั้งจึงสอนให้เขาเชื่อ
แล้วครั้งที่ผิดคือครั้งที่เขาเอาไปตัดสินใจ → **ออกแบบรอบโมเดล ไม่ใช่ที่ตัวโมเดล**

### กำแพงที่ 1 — `build_snapshot()` คิดเลขให้เสร็จก่อนเรียกโมเดล

ไม่ใช่แค่ยอดรวม แต่รวม**ค่าเฉลี่ยต่อบิล ต่อวัน สัดส่วน % วันที่ขายดีที่สุด/แย่ที่สุด
ส่วนต่างซื้อจริง-ตามสูตร** ทุกอย่างที่คำถามน่าจะอยากได้ คำนวณใน Python จาก rollup ตัวเดียว
กับที่หน้าจอใช้ → **โมเดลไม่เคยอยู่ในสถานะที่ต้องคิดเลขเพื่อตอบ**
เพราะโมเดลที่ถูกสั่งให้หารจะให้ตัวเลขเสมอ แต่ไม่ได้ให้ตัวเลขที่ถูกเสมอ

เป็นฟังก์ชัน pure (รับของที่ fetch มาแล้ว) → เทสต์ได้โดยไม่ต้องมี database
และ**การตัดสินใจว่าอะไรออกจากร้านบ้างอยู่ที่เดียว** ไม่กระจายตาม endpoint

### กำแพงที่ 2 — `verify_numbers()` ตรวจคำตอบย้อนกลับ

ตัวเลขในคำตอบที่ข้อมูลอธิบายไม่ได้ → **รายงาน ไม่ใช่บล็อก** (หลักเดียวกับ `uncosted_menus`)
- ข้าม < 100 เพราะ % และจำนวนนับเป็นของที่ derive ได้จริง จะกลบสัญญาณจริง
- ยอมคลาด 1% เพื่อให้ "ประมาณ 12,000" จาก 12,043 ไม่ถูกหาว่าแต่ง
- ดึงตัวเลขในสตริงวันที่มาเป็นค่าที่อนุญาตด้วย ไม่งั้น "สิงหาคม 2026" โดนแจ้งว่าแต่งปี

**ข้อจำกัดที่ต้องรู้:** จับ "ตัวเลขที่ไม่มีในข้อมูล" ได้ แต่จับ "เลขคณิตที่โมเดลคิดเอง" ไม่ได้
— ซึ่งเป็นเหตุผลที่กำแพงที่ 1 ต้องคำนวณมาให้ครบ ทั้งสองอันเป็นดีไซน์เดียวกัน

### caveats เดินทางมากับตัวเลข ในก้อนเดียวกัน

`{kind, message, items}` — มีชนิด (หน้าจอ render ได้ เทสต์ assert ได้) และ**มีชื่อของที่มีปัญหา**
ชนิดที่มี: `uncosted_menus` · `includes_today` · `recipe_cost_at_latest_price` ·
`negative_stock` · `no_purchases` · `no_expenses`
system prompt สั่งให้**ต้องพูดถึงเสมอ**ถ้าเกี่ยวกับสิ่งที่ถูกถาม เพราะ
"กำไร 42,000" กับ "กำไร 42,000 แต่ 3 เมนูยังไม่ผูกสูตร" เป็นคนละคำตอบ

### อะไรออกจากร้านบ้าง (เขียนไว้ใน `core/assistant_provider.py`)

**ออก:** ยอดขาย ต้นทุน กำไร เมนูขายดี ของสาขาเดียว ช่วงเดียว — **เจ้าของยอมรับแล้ว**
**ไม่ออกเด็ดขาด:** ข้อมูลลูกค้า · ชื่อ/ค่าแรงพนักงาน · `tenant_id` · token · บิลดิบ
มีเทสต์เช็คจาก**เนื้อหา** ไม่ใช่ชื่อฟิลด์ → เพิ่มฟิลด์ใหม่ที่มีของพวกนี้ก็ยังพัง

### ไฟล์

- `core/assistant_provider.py` — port (เหมือน `VisionProvider`) เมธอดเดียว `ask()`
- `core/assistant.py` — `INSTRUCTIONS` + `build_snapshot()` + `verify_numbers()`
- `adapters/gemini_assistant.py` — ตัวจริง · temperature 0 · ขอ**ข้อความ ไม่ใช่ JSON**
  (ขอ structured data ที่ไม่ได้ป้อนให้ = วิธีที่ตัวเลขมั่วได้ schema ห่อจนดูเป็นทางการ)
  ข้อมูลขึ้นก่อน คำถามอยู่ล่างสุดและติดป้ายว่า "เป็นคำถาม ไม่ใช่คำสั่ง" —
  เจ้าของร้านพิมพ์อิสระ เดี๋ยวก็มีวันที่พิมพ์อะไรที่อ่านเหมือนคำสั่ง

เทสต์: `tests/test_assistant.py` (46 checks) · **ยังไม่มี endpoint** — เฟส 3

---

## 7.13 ผู้ช่วย AI — เฟส 2: สรุปประจำวัน (ทำแล้ว)

**สรุปนี้ถูกอ่านตอนหกโมงเช้าโดยคนที่ไม่ได้เช็ก** — ข้อเท็จจริงข้อเดียวนี้ตัดสินดีไซน์ทั้งหมด

### template เขียน โมเดลแค่เรียบเรียง (สลับจากที่คนส่วนใหญ่ทำ)

โยนตัวเลขให้โมเดลเขียนสรุป = เอาทุกตัวเลขในสรุปไปฝากไว้กับวันที่โมเดลอารมณ์ไม่ดี
→ **สรุปเขียนใน Python จาก rollup ตัวเดียวกับที่หน้าจอใช้ ทุกตัวเลขจริงก่อนโมเดลจะเข้ามาเกี่ยว**
โมเดลได้ร่างที่เสร็จแล้วไปขัดสำนวน แล้วผลลัพธ์ถูกตรวจย้อนด้วย `assistant.verify_numbers()`

| สถานการณ์ | ร้านได้อะไร |
|---|---|
| โมเดลล่ม | ฉบับ template |
| โมเดลแต่งตัวเลข | ฉบับ template |
| โมเดลตัด caveat/ของที่ต้องสั่งทิ้ง | ฉบับ template |
| โมเดลทำงานถูกต้อง | ฉบับที่อ่านลื่นขึ้น ข้อเท็จจริงเดิมเป๊ะ |

**ไม่มีทางที่ร้านจะได้ตัวเลขผิด** · `polish_status` = `not_attempted`/`ok`/`rejected`/`failed`
บอกได้ว่า "ไม่เคยถาม" ต่างจาก "ถามแล้วโยนคำตอบทิ้ง" — คนละปัญหากัน

### `_dropped_warnings()` — กันการขัดสำนวนจนกลืนคำเตือน

เทียบจาก**ชื่อของ** (ชื่อวัตถุดิบที่ต้องสั่ง / ชื่อเมนูที่ยังไม่ผูกสูตร) ไม่ใช่ถ้อยคำ
เพราะการเปลี่ยนถ้อยคำคือหน้าที่ของมัน แต่ชื่อของที่ต้องสั่งคือคำที่ต้องรอด
การเขียนให้สวยจนคำเตือนหายคือการแก้ที่ทำให้สรุปแย่กว่าไม่ขัดเลย

### กฎที่เหลือ

- **วันที่ไม่มียอดขาย พูดตรง ๆ ว่าไม่มี** ห้ามหาแง่ดีมาใส่
  และ**ไม่คำนวณ % เทียบ** ด้วย — ร้านปิดไม่ใช่ "ยอดตก 100%" แต่เป็นประโยคที่โมเดล
  ที่ได้ตัวเลขนั้นไปจะเขียนให้ทันที (เทสต์จับเจอตอนเขียน ไม่ได้ตั้งใจดักไว้)
- **ค่าเฉลี่ยฐานไม่นับวันที่ปิดร้าน** ไม่งั้นร้านที่ปิดทุกวันจันทร์จะดูเหมือนทรุดทุกวันอังคาร
- **มีประวัติน้อยกว่า 2 วันไม่เทียบเลย** (หลักเดียวกับ `compare_previous`)
- **ขัดสำนวนซ้ำต้องไม่เห็นฉบับที่ตัวเองขัดไปแล้ว** ไม่งั้นเพี้ยนสะสมทุกรอบ

### สร้างแบบ lazy ไม่ใช่ตั้งเวลา

สร้างครั้งแรกที่มีคนขอ แล้วเก็บที่ `daily_briefs/{date}` → คนที่สองที่เปิดเช้านั้น
(และคนเดิมที่เปิดซ้ำตอนเที่ยง) อ่าน 1 doc · **`delete_daily()` ลบ brief ของวันนั้นไปด้วย**
— วันที่ควรสร้างใหม่คือ brief ที่ควรสร้างใหม่ ไม่งั้นตัวเลขบนจอกับในสรุปไม่ตรงกัน
และสรุปคือฝั่งที่ไม่มีใครไปเช็กซ้ำ

**ทำไมไม่ตั้ง cron:** ต้องมี scheduler ที่แอปนี้ยังไม่มี · จะรันให้สาขาที่ไม่มีใครดู ·
และจะพังเงียบ ๆ ตอนตีสี่ · การที่มีคนขอคือหลักฐานว่ามีคนอยากได้

### ไฟล์

- `core/daily_brief.py` — `build()` (pure) · `polish()` (ไม่ raise เลย) · `display_text()`
- `GET /api/{store_id}/brief?date=` — ดีฟอลต์เมื่อวานตามเวลาร้าน
  วันที่ยังไม่จบตอบ `{ready: false}` ไม่ใช่ error (หน้าจอจะได้ไม่ต้องจับ exception)
- `Dashboard.jsx` การ์ด "เมื่อวาน" — **ไม่มีอะไรจะบอกก็ไม่ render อะไรเลย**
  การ์ดที่ขึ้นว่า "ยังไม่มีสรุป" ทุกเช้า คือการ์ดที่คนเรียนรู้ที่จะข้าม
  แล้วก็จะข้ามในเช้าที่มันมีอะไรจะบอกจริง ๆ
- ไม่มี `GEMINI_API_KEY` = ไม่เรียกโมเดลเลย ไม่ใช่สถานะพิการ สรุปสมบูรณ์อยู่แล้ว

เทสต์: `tests/test_daily_brief.py` (40 checks)

---

## 7.14 ผู้ช่วย AI — เฟส 3: ถาม-ตอบ (ทำแล้ว)

### ⚠️ ทำไม่ตรงตามแผนที่คุยกันไว้ — และทำไม

แผนเดิมคือ **tool 5 ตัวให้โมเดลเรียกดึงช่วงเวลาที่มันคิดว่าคำถามพูดถึง** ไม่ได้ทำแบบนั้น

เหตุผล: **tool ที่คืนข้อมูลดิบ = ผลักโมเดลกลับไปคิดเลข** ซึ่งเป็นสิ่งเดียวที่ดีไซน์ทั้งหมด
พยายามหลีกเลี่ยง → tool ก็ต้องคืน "สรุปที่คำนวณเสร็จแล้ว" อยู่ดี
ถึงตรงนั้น สิ่งเดียวที่ tool ให้เพิ่มคือ **ตอบช่วงเวลาที่ไม่มีใครเลือก** ได้

แต่คนที่อยากถามถึงสงกรานต์ปีที่แล้ว **เลือกสงกรานต์ปีที่แล้วได้** — นั่นคือ date control
ไม่ใช่ protocol และดีกว่าตรงที่**เขาเห็นว่าคำตอบพูดถึงช่วงไหน**อยู่บนจอ
แทนที่จะต้องเชื่อว่าโมเดลตีความช่วงเวลาถูก

→ **ช่วงเวลาเป็นพารามิเตอร์ ไม่ใช่สิ่งที่โมเดลเดา** ถ้าคำถามต้องการช่วงที่ไม่ได้เลือกไว้
คำตอบที่ซื่อสัตย์คือ "ไม่มีข้อมูลช่วงนั้น" ซึ่งกฎข้อ 2 ของ INSTRUCTIONS บังคับอยู่แล้ว

**ยังทำ tool loop ทีหลังได้** ไม่ได้ปิดทาง — แต่ควรมีคำถามจริงที่ตอบไม่ได้ก่อน

### สิ่งที่ส่งให้โมเดล

`build_context()` = ช่วงที่ถาม + **ช่วงก่อนหน้ายาวเท่ากัน** + `change` (ส่วนต่างที่ลบมาแล้ว)
+ `series` (ยอดรายวัน สำหรับ "วันไหนขายดีสุด")

- **ลบเลขให้เสร็จตรงนี้** เพราะโมเดลที่ถูกถามว่า "เปลี่ยนไปเท่าไหร่" จะให้ตัวเลขเสมอ
- **`sales_direction` เป็นคำ** (`up`/`down`/`flat`) ไม่ให้โมเดลอ่านทิศจากเครื่องหมาย
  แล้วอ่านกลับด้านตอนกำไรติดลบ
- **`_trim_previous()`** ตัด `stock` กับ `caveats` ออกจากช่วงก่อนหน้า —
  stock เป็นยอด ณ ตอนนี้ ถ้าใส่ทั้งสองช่วงจะมี "stock" สองก้อนให้โมเดลเอาไปเทียบกัน
  เหมือนก้อนหนึ่งเป็นของอดีต

### `answer()` ไม่เคย raise

ปฏิเสธก่อนเสียเงิน: คำถามว่าง · คำถามยาวเกิน 400 ตัวอักษร
(คำถามที่ยาวพอจะซ่อนคำสั่งไว้ข้างในไม่ใช่คำถามเรื่องยอดขาย)
provider ล้ม → `{ok: false, error: ...}` ข้อความไทย ไม่ใช่ stack trace

**ตัวเลขที่ตรวจไม่ผ่าน = แสดงให้คนอ่านเห็น ไม่ใช่กลืน** คำตอบที่มีตัวเลขที่ข้อมูลอธิบายไม่ได้
ยังอ่านได้อยู่ แค่ไม่ควรเอาไปตัดสินใจโดยไม่เช็ก และมีแต่คนอ่านเท่านั้นที่บอกได้

### เพดานที่ตั้งไว้

- **`ASSISTANT_DAILY_LIMIT` = 50 คำถาม/ธุรกิจ/วัน** (env override ได้)
  นับเฉพาะคำถามที่ไปถึง provider จริง — กล่องว่างที่ถูกปฏิเสธไม่กินโควต้าใคร
  เก็บที่ `tenants/{id}/assistant_usage/{date}` ด้วย `Increment`
- **`MAX_ASK_DAYS` = 400 วัน** — 1 ปี = 365 แถว บวกช่วงก่อนหน้าอีก 365
  เกินกว่านั้นไม่ใช่คำถามที่ใครตั้งใจพิมพ์ (ช่อง date ค้างที่ค่าต่ำสุด / คีย์ปีผิด)
  แต่จะเงียบ ๆ แพงกว่าการใช้งานปกติทั้งเดือน
- recipes + materials ดึงครั้งเดียวใช้ทั้งสองช่วง (อย่างละ 1 collection read)

### หน้าจอ

`/assistant` "ถามข้อมูลร้าน" — เลือกช่วงวันที่ได้ · ปุ่มคำถามสำเร็จรูป 4 อัน ·
แสดง caveats ของช่วงนั้นใต้คำตอบ · แสดงตัวเลขที่ตรวจไม่ผ่านเป็นคำเตือน ·
บอกโควต้าที่ใช้ไปวันนี้ · `needs: 'view_money'` ทั้ง nav และ route (backend บังคับซ้ำเอง)

เทสต์: `tests/test_assistant.py` (72 checks รวมเฟส 1)

---

## 7.15 รีวิวหลังเจ้าของปรับผู้ช่วยเอง — แก้สามจุด (ทำแล้ว)

เจ้าของเพิ่ม `advisor.py` + `question_intent.py` + หน้า `/assistant` ใหม่ (3 commit)
โครงดี — โดยเฉพาะ **ส่งแค่คำถามเก่า ไม่ส่งคำตอบเก่า** แล้วตรวจกับ `context` ไม่ใช่
`model_context` (ตัวเลขที่คนพิมพ์ในคำถามเก่าจึงไม่กลายเป็นตัวเลขที่ได้รับอนุญาต)
และ **`costed: False`** แทนที่จะให้ต้นทุนเป็น 0 (ไม่งั้นเมนูที่ยังไม่ผูกสูตรจะกลายเป็น
เมนูกำไรดีที่สุดของร้าน) · รีวิวแล้วเจอสามจุดที่ต้องแก้

### 1. `verify_numbers` อ่อนลงเกือบเท่าตัว — แก้แล้ว

`menu_performance(limit=0)` ใส่ qty/revenue/unit_cost/gross_profit/margin ของ**ทุกเมนู
ทั้งสองช่วง** ลง context → กองตัวเลขที่อนุญาตใหญ่ขึ้น 5 เท่า → ตัวเลขมั่วไป "ตรง"
กับอะไรสักอย่างในระยะ 1% เสมอ

วัดจากร้านจำลอง 80 เมนู 30 วัน (สุ่มตัวเลขที่ไม่มีในข้อมูลเลย):

| | payload | กองตัวเลข | เลข 4 หลักหลุด | เลข 3 หลักหลุด |
|---|---|---|---|---|
| ก่อนมี performance | 5,543 | 108 | 34.7% | — |
| หลังเจ้าของแก้ | 40,887 | 499 | **69.3%** | 34.7% |
| **หลังแก้รอบนี้** | **12,829** | **194** | **42.7%** | **9.0%** |

**ทางแก้: เก็บลิสต์เต็มไว้ฝั่ง Python ส่งให้โมเดลแค่ top 12 + เมนูที่ถูกเอ่ยชื่อในคำถาม**
(`assistant.MODEL_MENU_LIMIT` + `menus_named_in()` + `_for_model()`)
เหตุผลเดิมที่ส่งทั้งหมด ("คำถามอาจถามถึงเมนูที่ขายน้อยที่สุด") ยังได้รับการตอบสนองครบ
เพราะ `_mentioned_menu` หาจาก**ลิสต์เต็ม**ก่อน แล้วเมนูนั้นค่อยเดินทางไปด้วย
(เทสต์: เมนูอันดับ 47 จาก 80 ถูกถามถึง → ไปด้วย และตัวเลขของมันกลับมาเป็นตัวเลขที่อนุญาต)

**ต้องพูดให้ตรง:** กำแพงนี้ไม่เคยแข็ง 34.7% ก็สูงมากอยู่แล้ว การแก้ครั้งนั้นขยายรูเดิม
ไม่ได้เจาะรูใหม่ และ 42.7% ที่เหลือก็ยังสูงกว่าเดิม เพราะ 12 เมนูที่ส่งไปมีตัวเลขจริงของมัน
**ทางแก้ที่แท้จริงคือตรวจแบบรู้ว่าตัวเลขคู่กับป้ายอะไร** ไม่ใช่เทียบกับถุงตัวเลข — ยังไม่ทำ
(ลด tolerance จาก 1% เหลือ 0.5% ลดเหลือ 28.5% แต่จะเริ่มเตือน "ประมาณ 230,000" ที่ปัดจาก
231,510 — ยังไม่เปลี่ยน รอตัดสินใจ)

**`performance_note` สำคัญ:** บอกโมเดลว่ายังมีอีกกี่เมนูที่ไม่ได้แสดง
ถ้าไม่บอก โมเดลที่เห็น 12 แถวจะรายงานว่าร้านมี 12 เมนู

### 2. payload 41k → 12.8k ตัวอักษร

86% ของ payload เดิมคือ `menus.performance` สองชุด (`_trim_previous` ตัด `stock`
กับ `caveats` แต่ไม่ได้ตัด `menus`) กระทบทั้งค่า token เวลาตอบ และ**ปริมาณข้อมูลที่ออกจากร้าน**
ซึ่งเป็นเรื่องที่เขียนกำกับไว้ใน `assistant_provider.py` ว่าคนเพิ่มฟิลด์ควรรู้ตัว
แก้ด้วยข้อ 1 พร้อมกัน

### 3. `list_expenses` อ่านทั้ง collection — **บั๊กที่ติดมาตั้งแต่ `5007f11`**

`_period_snapshot` ดึงรายจ่ายทั้งหมดแล้วกรองใน Python — พันธุ์เดียวกับข้อ 7.9 เป๊ะ ๆ
และ `/insights` ที่เพิ่มใหม่มาเรียกซ้ำอีกชุด

| | reads |
|---|---|
| ร้านที่บันทึกรายจ่าย 2 ปี (480 รายการ) ต่อ 1 คำถาม | **960** |
| เปิดหน้า + ถาม 1 คำถาม | **1,920** |
| **หลังแก้ (ถามเดือนเดียว)** | **21** |

ใส่ range filter ให้ `list_expenses(start=, end=)` · **ปลาย range บวก `\uf8ff`**
เพราะไม่มีอะไร normalize ฟิลด์นี้ตอนเขียน แถวที่บันทึกเป็น timestamp เต็มจะหลุดจากทุกเดือน
(เทสต์คุมไว้แล้ว) · แถวที่ไม่มีวันที่ยังถูกตัดออกเหมือนเดิม — ไม่มีวันที่ = ไม่อยู่ในเดือนไหน

### 4. `/ask` คืน `recommendations` มาด้วย

เดิม `question_intent.analyze()` คำนวณ recommendations อยู่แล้วทุกคำถาม แต่หน้าจอไปดึง
`/insights` ซึ่งคำนวณอันเดียวกัน**จากการอ่านข้อมูลคนละชุด** ตอนนี้คำตอบพก recommendations
กลับมาเลย หน้าจอไม่ต้องยิงซ้ำ และการ์ดกับคำตอบพูดถึงตัวเลขชุดเดียวกันแน่นอน
(ที่ยังซ้ำคือ /insights ตอนเปิดหน้าครั้งแรก — ต้องใช้แคชข้อ 7.10 ถึงจะยุบได้จริง)

### 5. `run_all.py` นับเทสต์ไฟล์ใหม่เป็น 0

ไฟล์ใหม่พิมพ์ `9/9 tests passed` แต่ตัวนับหาแต่คำว่า `checks passed`
ไฟล์ยัง gate ด้วย exit code อยู่ ไม่อันตราย แต่ชุดเทสต์จะ**หดลงเงียบ ๆ โดยที่ตัวเลขหน้าตาเหมือนเดิม**
→ นับทั้งสองคำ · 820 → **854 checks**

### ที่ยังไม่ได้แก้ (เล็ก)

- **กฎที่คมที่สุดของงานนี้ไม่มีเทสต์** — วงจร tracking (ช่วงต้องยาวเท่ากัน / เริ่มหลัง baseline /
  ห้ามซ้ำถ้ายัง active / ห้ามรวมวันนี้) อยู่ใน endpoint ล้วน ๆ ซึ่งเทสต์ชุดนี้เข้าไม่ถึง
- `candidates.sort(key=lambda item: (-item.pop("_score"), ...))` — `pop` ใน sort key
  ทำงานได้เพราะ Python เรียก key ครั้งเดียวต่อ item แต่ sort ลิสต์เดิมซ้ำเมื่อไหร่ crash
- PATCH ย้าย `completed` กลับเป็น `planned` ได้โดย `evaluation` ยังค้างอยู่

---

## 7.16 ลองใช้จริงแบบเจ้าของร้านใหม่ — แก้สามจุดแรก (ทำแล้ว)

รันแอปจริงทั้งชุด (Auth emulator + backend + frontend + Playwright) แล้วสมัครเป็นร้านใหม่
กดเองทุกหน้า · เจอ 8 เรื่อง เลือกแก้ 3 อันแรกก่อน

### 1. หน้าแรกหลังสมัครเสร็จคือทางตัน — แก้แล้ว

**ทั้ง 11 หน้า** ขึ้นข้อความสีเทาบรรทัดเดียว `เลือกสาขาในหน้าตั้งค่าก่อน` ไม่มีปุ่ม ไม่มีลิงก์
และ **"หน้าตั้งค่า" ไม่มีอยู่ในเมนู** (ของจริงคือ เพิ่มเติม → ตั้งค่าระบบ → เชื่อมต่อ & สาขา)

→ `components/SetupGate.jsx` **หนึ่งบรรทัด หนึ่งปุ่ม** แทนที่ประโยคเปล่าทั้ง 11 ที่
หน้าแรกเก็บหัวข้อ + ชื่อร้านไว้ด้วย — การ์ดลอยเดี่ยวบนพื้นที่ว่างอ่านเหมือนหน้า error
มีชื่อร้านอยู่ข้างบนถึงจะอ่านเหมือน "ร้านของคุณอยู่นี่ เหลืออีกขั้นเดียว"

### 2. ทุกเช้าหน้าแรกขึ้น ฿0 ↓100% — แก้แล้ว

กฎเดิม "เทียบกับช่วงยาวเท่ากันที่ติดกันก่อนหน้า" พอเป็น "วันนี้" มันข้ามเที่ยงคืนไปเจอ**ช่วงเย็นเมื่อวาน**

```
เก้าโมงเช้า → เทียบกับ เมื่อวาน 15:00–24:00 (ช่วงพีคของร้านอาหาร)
```

ร้านที่ขายปกติจะเห็นตัวเลขแดงติดลบ**ทุกเช้า ตลอดไป** และเป็นเลขคณิตที่เถียงไม่ได้
ซึ่งคือความผิดแบบที่แย่ที่สุด — ไม่มีอะไรให้สังเกต

→ `comparison_window()` : ช่วง **≤ 1 วัน** เทียบ **เวลาเดียวกันของเมื่อวาน** ·
ยาวกว่านั้นใช้กฎเดิม (ถูกอยู่แล้ว) · **ไม่เพิ่มค่าอ่านเลย** เดิมก็อ่านหน้าต่างที่สองอยู่แล้ว

**ติดป้ายบนจอด้วย** — `compare` คืน `basis` มา หน้าจอเขียน "เทียบเวลาเดียวกันเมื่อวาน" /
"เทียบช่วงก่อนหน้า" · เปอร์เซ็นต์ที่ไม่บอกว่าเทียบกับอะไร คือเปอร์เซ็นต์ที่ตีความผิดได้ฟรี

### 3. การ์ดเริ่มต้นใช้งาน — ทีละอย่าง ไม่ใช่ checklist

**เจ้าของร้านตัดสินใจเอง:** เอาแค่ถึง "เชื่อม Loyverse → ดึงข้อมูล" พอ ที่เหลือมีระบบเตือนอยู่แล้ว
และ**การใช้ครั้งแรกต้องไม่รู้สึกยาก เยอะ วุ่นวาย**

5 ขั้นบอกลูกค้าใหม่ว่า "มีการบ้าน 5 อย่างก่อนแอปนี้จะมีประโยชน์" — และมั่วขนาดงานด้วย
(วาง token 30 วินาที vs ผูกสูตรทั้งร้านหลายวัน อยู่ในลิสต์เดียวกันเหมือนเท่ากัน)

**เหลือ 2 ขั้นที่เรียงกัน = ตอนไหนก็มีของให้ทำอย่างเดียว → ไม่ต้องเป็น checklist เลย**
- ยังไม่เชื่อม → `SetupGate` (ทั้งหน้า ไม่ใช่การ์ด เพราะไม่มีอะไรอยู่ข้างหลังให้ดู)
- เชื่อมแล้วยังไม่เคยซิงก์ → การ์ด `FirstSync` หนึ่งบรรทัด + ปุ่ม "ดึงยอดขาย" (กดในการ์ดเลย)
- ซิงก์แล้ว → หายไป เหลือตัวเลขจริงของร้าน **นั่นคือรางวัล ไม่ใช่การบ้านข้อถัดไป**

**ก่อนซิงก์ครั้งแรกซ่อนสรุปเมื่อวานกับ "ต้องจัดการ" ด้วย** — สรุปของวันที่ร้านไม่มีบันทึก
กับเตือนให้นับสต๊อกที่ยังไม่มี เป็นของที่มาเร็วเกินไปทั้งคู่

**รู้ว่ายังไม่เคยซิงก์จาก `never_synced`** ใน `/alerts` (= `get_sync_cursor()` เป็น None)
1 doc read · **สาขาที่ "วันนี้ไม่มีบิล" ไม่เท่ากับ "ไม่เคยซิงก์"** ร้านใหม่ที่เปิดวันอังคารเงียบ ๆ
ไม่ควรถูกบอกให้ไปแก้อะไรที่ไม่ได้พัง

**"ไว้ทีหลัง" ซ่อนหนึ่งวัน ไม่ใช่ตลอดไป** — เก็บ `onboarding_dismissed_at` (เวลา ไม่ใช่ `true`)
ใน settings เดียวกับ timezone → `/api/me` อ่าน doc นั้นอยู่แล้ว **ไม่เพิ่มค่าอ่าน**
ร้านที่เชื่อมไว้ก่อนเปิดหนึ่งสัปดาห์ไม่ต้องเห็นทุกชั่วโมง และการ์ดที่ปิดแล้วหายไปเลยก็เป็นทางตันอีกแบบ

> นี่เป็น state เดียวของ setup ที่ยอมให้เก็บ — "ขั้นนี้เสร็จหรือยัง" เป็นข้อเท็จจริงของร้าน
> อ่านสดทุกครั้ง แต่ "อยากให้เตือนอีกไหม" เป็นความตั้งใจของคน ซึ่ง derive ไม่ได้

### 4-8. ที่เหลือ — แก้แล้วทั้งหมด

**หน้าตั้งค่า: พับเครื่องมือซ่อม 4 อันลงไป** (`ความถี่ซิงก์ · ย้ายข้อมูลสต๊อก · คำนวณยอดคงเหลือใหม่ ·
ซิงก์ตอนนี้/รีเซ็ตจุดซิงก์`) ไว้ใต้ปุ่ม "เครื่องมือขั้นสูง" · ร้านใหม่เหลือเห็น 3 การ์ด
**พับ ไม่ใช่ลบ** — วันที่ต้องใช้คือวันที่ต้องใช้จริง ๆ ตามหาใน release note แย่กว่ากดเพิ่มหนึ่งที

**ข้อความ error ตอนเชื่อมต่อ: แยกสาเหตุ** (`_connect_failure()`)

| เจอ | บอกว่า |
|---|---|
| 401/403 | token ใช้ไม่ได้ ลองสร้างใหม่ |
| 402 | แพ็กเกจ Loyverse ไม่รองรับ API |
| 429 | โดนจำกัดคำขอ รอสักครู่ |
| ต่อไม่ติดเลย | **"เช็คอินเทอร์เน็ตแล้วลองใหม่ (ยังไม่ต้องสร้าง token ใหม่)"** |
| status อื่น | บอกเลข status ไม่โยน traceback |

ของเดิมบอกให้ "เช็ค token" ทุกกรณีแล้วแปะ exception ดิบต่อท้าย — คนเน็ตหลุดจะนั่งสร้าง token
ใหม่วนไปเรื่อย ๆ แก้ผิดจุดตลอด

**บั๊ก CSS ปุ่มแดง:** `button[style*="var(--accent)"]` → `button[style*="background: var(--accent)"]`
selector เดิมจับ substring ที่ไหนก็ได้ใน inline style ลิงก์ที่เขียน `color: var(--accent)`
เลยกลายเป็นปุ่มแดงทึบตัวหนังสือขาว **วัดหลังแก้: `rgba(0,0,0,0)` / `rgb(185,71,46)`** ถูกต้องแล้ว
(ปุ่มที่ตั้ง background เป็น accent จริง ๆ ยังทำงานเหมือนเดิม)

**หน้าสูตรอาหารตอนไม่มีเมนู:** เดิมเป็นการ์ดเปล่า → ใส่ empty state บอกว่าเมนูมาจาก Loyverse
*(หมายเหตุ: หน้าเปล่าที่ผมเจอตอนเทสต์เป็นเพราะ POS ปลอมของผมส่ง field ผิด ไม่ใช่บั๊กจริง
พอ stub ถูก หน้านี้แสดงเมนูครบปกติ — แต่ empty state ก็ยังควรมีอยู่ดี)*

**caveat `no_expenses` บนหน้ารายรับรายจ่าย:** ขึ้นเมื่อมีรายรับแต่รายจ่ายรวมเป็น 0 —
"กำไรสุทธิที่เห็นจึงเท่ากับรายรับ ยังไม่ใช่กำไรจริง" · เป็นสาเหตุที่ใหญ่กว่าเรื่องสูตรเสียอีก
และวันแรกทุกร้านจะเจอ

**`/api/admin/whoami` 404 ทุกหน้า:** `/api/me` คืน `is_admin` มาเลย (super admin เป็น env var
ไม่ใช่การ query — ไม่มีค่าอ่านเพิ่ม) · console แดงทุกหน้าคือวิธีทำให้ error จริงไม่มีใครเห็น

### วิธีรันแอปทั้งชุดใน sandbox (ไว้ใช้ครั้งหน้า)

- **Firestore emulator ใช้ไม่ได้** — ต้องโหลด jar จาก `storage.googleapis.com` ซึ่ง egress บล็อก
  → ใช้ `tests/fake_firestore.FakeDb` แทน แล้ว `m.root_store.db = FakeDb()` หลัง import
- **Auth emulator ใช้ได้** (เป็น node ล้วน) แต่ต้องปิด `emulators.ui` ใน firebase.json ก่อน
  (UI ก็โหลด zip จาก googleapis เหมือนกัน)
- Loyverse ต่อไม่ได้ → monkeypatch ทั้ง `m._adapter_for` **และ** `m.LoyverseAdapter`
  (`_add_connection` สร้าง adapter เองตรง ๆ ไม่ผ่าน `_adapter_for`)
- ขับด้วย Playwright + chromium ที่ `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`
- **ระวัง:** แทรก import ด้วยสคริปต์ ห้ามหา "บรรทัดสุดท้ายที่ขึ้นต้นด้วย import"
  เพราะ import หลายบรรทัดจะทำให้แทรกกลางวงเล็บปีกกา ต้องหาบรรทัดที่**ปิด** import

---

## 7.17 ผู้ช่วยตอบแคบเกินไป — ให้มันขอข้อมูลเองได้ (ทำแล้ว)

**เจ้าของร้านบอกว่า "ตอบไม่กว้าง เหมือนตอบได้แค่สิ่งที่แสดงอยู่ในการ์ดที่มีอยู่แล้ว"**
ไล่คำถามจริง 11 ข้อ ตอบได้ 5 · ปัญหาไม่ได้อยู่ที่โมเดล อยู่ที่สิ่งที่เราส่งให้มัน

### สามสาเหตุ

1. **ของที่คำนวณไว้แล้วถูกทิ้ง** — `build_deep_analysis()` ทำงานทุกครั้งที่ถาม คำนวณ
   `revenue_declines` `lowest_margin` `biggest_gross_profit` `signals` ครบ
   **แต่ใช้แค่ 2 intent จาก 9 ที่เหลือคำนวณทิ้ง** ไม่มีอันไหนเข้า context เลย
2. **`response_contract` บังคับโครงคำตอบ 6 ช่อง** + INSTRUCTIONS ข้อ 6 สั่งว่า
   "ใช้ข้อสรุปตามนั้น ห้ามเปลี่ยนเอง" → โมเดลกลายเป็นคนกรอกฟอร์ม
   **นี่คือสาเหตุที่มันอ่านเหมือนอ่านการ์ด**
3. **ช่วงเวลาเปลี่ยนตามคำถามไม่ได้** ทุกคำถามพูดถึงช่วงที่ช่องวันที่เลือกไว้เท่านั้น

### ผมคิดผิดตอนเฟส 3 — ต้องบันทึกไว้

ตอนนั้นตัด tool ทิ้งด้วยเหตุผลว่า "tool ที่คืนข้อมูลดิบก็ผลักโมเดลกลับไปคิดเลข
สุดท้ายต้องคืนสรุปที่คำนวณเสร็จอยู่ดี แล้วมันก็ไม่ต่างจากแพ็กมาให้ล่วงหน้า"

**ครึ่งแรกถูก ครึ่งหลังผิด** — สมมติฐานที่ผิดคือ "มุมที่คนอยากได้มีไม่กี่มุม เดาล่วงหน้าได้"
พอลองไล่คำถามจริงถึงเห็นว่าไม่ใช่ **คุณค่าของ tool ไม่ใช่ข้อมูลดิบ
แต่คือมันครอบคลุมพื้นที่แทนที่จะนับทีละอัน**

### `core/shop_query.py` — โมเดลเขียนใบสั่ง Python เป็นคนคำนวณ

```json
{"dataset":"sales","from":"2026-07-01","to":"2026-07-31",
 "group_by":"weekday","metrics":["sales","bills"],"filter":{"channel":"grab"}}
```

**ไม่มี eval ไม่มีโค้ดจากโมเดล** — ทุกฟิลด์ตรวจกับ allowlist · โมเดลเลือกแค่ว่า*อยากได้มุมไหน*
ตัวเลขทุกตัวคำนวณใน Python → **กฎ "ห้ามคิดเลข" ไม่ต้องแก้สักข้อ**

**สิ่งที่ปฏิเสธ ไม่ใช่ตอบด้วยของใกล้เคียง** (สำคัญที่สุดในไฟล์นี้)

| ขอ | ทำไมปฏิเสธ |
|---|---|
| เมนูของช่องทางใดช่องทางหนึ่ง | rollup รู้ยอดรวมของแต่ละช่องทาง กับยอดของแต่ละเมนู **แต่ไม่รู้ว่าช่องทางไหนขายเมนูไหน** → ถ้าตอบไปจะกลายเป็นเมนูขายดีของทั้งร้าน ซึ่งเป็นคำโกหกที่มีตัวเลขติดมาด้วย |
| กำไรของช่องทางเดียว | เหตุผลเดียวกัน |
| จำนวนบิลต่อเมนู | หนึ่งบิลมีได้หลายเมนู เป็นปริมาณที่ไม่มีอยู่จริง |
| filter ที่ไม่รู้จัก | **ปฏิเสธ ไม่ใช่ข้ามไป** — filter ที่ถูกข้ามเงียบ ๆ เปลี่ยน "เฉพาะ Grab" เป็น "ทั้งหมด" |

**totals ที่เป็น totals จริง** — ผลรวมของค่าเฉลี่ยรายวัน 7 ตัวไม่ใช่ค่าเฉลี่ย
และผลรวมของ margin 7 ตัวไม่ใช่ margin · ทั้งคู่หน้าตาเหมือน total เป๊ะ
→ `avg_bill` กับ `gross_margin_pct` **คำนวณใหม่จากยอดรวม ไม่ใช่บวกแถว**

**เมนูที่ไม่มีสูตร กำไรเป็น `None` ไม่ใช่ 0 และเรียงไปท้ายเสมอ** —
ไม่งั้น "เมนูกำไรน้อยที่สุด" จะกลายเป็นเมนูที่ยังไม่รู้กำไร

### ต่อเข้ากับโมเดล

- `AssistantProvider.converse()` **ไม่ใช่ abstract** และ default ตกไปที่ `ask()` —
  provider ที่ไม่รองรับ function calling ต้องตอบได้อยู่ ครบน้อยลง ไม่ใช่พังไปเลย
- Gemini: loop `functionCall` → `functionResponse` สูงสุด **3 รอบ** แล้วถามซ้ำแบบไม่มี tool
  เพื่อให้ได้คำตอบจากที่รวบรวมมา ไม่ใช่ไม่ได้อะไรเลย
- **spec ที่ถูกปฏิเสธส่งกลับเป็น "เหตุผล" ไม่ใช่ exception** — "ระบบไม่ได้เก็บว่าแต่ละช่องทาง
  ขายเมนูไหน" คือสิ่งที่คนถามสมควรได้ยิน และโมเดลจะบอกต่อได้ก็ต่อเมื่อมันถูกบอก
- `verify_numbers` ตรวจกับ **context + ผลลัพธ์ทุก query** (ตัวเลขจาก query คำนวณโดย Python
  ก็เป็นข้อเท็จจริงของร้าน) แต่ยังไม่ตรวจกับตัวเลขที่คนพิมพ์ในคำถามเก่าเหมือนเดิม
- `_query_window()` — query ออกนอกช่วงบนจอได้ (นั่นคือประเด็นของมัน) แต่ยังติดเพดาน
  `MAX_ASK_DAYS` และ**ผูกกับสาขา+ธุรกิจนี้เสมอ** spec ไม่มีช่องให้ระบุร้าน

### ทางตันไม่จบที่ "ไม่มีข้อมูล" อีกต่อไป

INSTRUCTIONS เขียนใหม่ แยกทางตันเป็น 3 แบบ: **ไม่ได้เก็บเลย** (บอกว่าต้องบันทึกอะไรเพิ่ม) ·
**เก็บแล้วแต่ยังน้อย** (บอกว่าอีกเมื่อไหร่จะดูได้) · **เก็บแล้วแต่ไม่แม่น** (ตอบพร้อม caveat ไม่ใช่ปฏิเสธ)

ให้มุมมองทั่วไปประกอบได้ **แต่ห้ามมีตัวเลขที่บรรยายร้านนี้ในส่วนที่เป็นความเห็นเด็ดขาด**
เช่น "ปกติหักค่าคอม 30% เมนู 50 บาทเหลือ 35" — แย่กว่าไม่ตอบ เพราะมันเฉพาะเจาะจง
ฟังดูมีเหตุผล และพูดถึงร้านของเขา ทั้งที่ไม่มีใครคำนวณมันเลย

### หน้าจอ: บอกว่าไปดูอะไรมา

ใต้คำตอบขึ้นบรรทัด `ดูจาก: ยอดขาย/จำนวนบิล ตามวันในสัปดาห์` — แปลจาก spec เป็นคำพูด
"ตัวเลขนี้มาจากไหน" คือคำถามที่คนถามกับทุกตัวเลขที่ตัวเองไม่ได้บวก และคำตอบสั้นพอจะพิมพ์

### ยืนยันแล้ว

รันจริงผ่าน endpoint จริง (Auth emulator + FakeDb + POS ปลอม 1,535 บิล ย้อนหลัง 70 วัน
เสาร์อาทิตย์ขายดีกว่า) ด้วย provider ปลอมที่ยิง query แบบที่โมเดลจริงจะยิง:

```
ถาม: วันไหนของสัปดาห์ขายดีที่สุด
ตอบ: วันอาทิตย์ขายดีที่สุด 21,905 บาท ส่วนวันศุกร์น้อยที่สุด 7,470 บาท
ค้น: ยอดขาย/จำนวนบิล ตามวันในสัปดาห์   ตรวจไม่ผ่าน: []
```

**คำถามนี้ก่อนหน้านี้ตอบไม่ได้เลย** · เทสต์ `tests/test_shop_query.py` (39) +
`test_assistant.py` (109) · รวมทั้งชุด **919 checks**

### ⚠️ ผมเข้าใจผิดเรื่องค่าคอม — แก้แล้ว

ผมสรุปว่า "ระบบไม่ได้เก็บค่าคอมเดลิเวอรี" **แล้วเขียนลงใน `describe()` ซึ่งส่งให้โมเดลทุกครั้ง**
เจ้าของร้านทักว่า **ค่าคอมออกแบบให้บันทึกในระบบรายจ่ายอยู่แล้ว**

นี่เป็นบั๊กที่แย่กว่าโค้ดพัง — schema ที่โกหกจะทำให้ผู้ช่วย**บอกร้านว่าบัญชีที่เขาทำอยู่ไม่มีอยู่จริง**
และคนใช้ไม่มีทางรู้ว่าคำตอบผิดเพราะอะไร

แก้เป็น:
- เอาออกจากรายการ "ไม่ได้เก็บไว้" · เขียนใน `expenses/คือ` ว่า**รวมค่าคอมของแอปเดลิเวอรี**
- เพิ่มหมวด **"ตอบได้แต่ต้องประกอบสองฝั่ง"** — กำไรของช่องทางเดลิเวอรี = ยอดขายช่องทางนั้น
  (dataset `sales` + filter channel) คู่กับค่าคอม (dataset `expenses` หาจากชื่อรายการ)
  **สั่งให้ยกมาทั้งสองตัวเลข ห้ามลบเอง** และบอกผู้ใช้ว่าจับคู่ค่าคอมจากชื่อ
- **ส่งชื่อรายจ่ายที่มีอยู่จริงในช่วงนั้นไปด้วย** (`ชื่อรายการที่มีอยู่จริงในช่วงนี้`)
  — รายจ่ายคือชื่อที่เจ้าของร้านพิมพ์เอง ไม่มีฟิลด์ไหนบอกได้ว่าอันไหนคือค่าคอม
  เห็นชื่อจริงถึงเปลี่ยนจากเดาเป็นหาเจอ · ชื่อพวกนี้ fetch มาแล้วสำหรับ snapshot อยู่แล้ว
  (แถมยุบ `list_expenses` ที่เคยเรียกซ้ำในคำขอเดียวให้เหลือครั้งเดียว)

**ข้อจำกัดที่เหลือจริง ๆ:** รายจ่าย**ไม่ได้ผูกกับช่องทางขายเชิงโครงสร้าง** การจับคู่อาศัยชื่อที่พิมพ์
("ค่าคอม Grab" vs "คอมมิชชั่นแกร็บ" vs "GrabFood fee") → prompt สั่งให้บอกผู้ใช้ทุกครั้งว่าจับคู่จากชื่อ

### `hours` ใน rollup — "ช่วงไหนของวันขายดี" ตอบได้แล้ว

เพิ่ม `hours: {"19": {total, count}}` ลงใน rollup · **เก็บแบบ sparse ไม่ใช่ 24 ช่อง**
ร้านเปิดสิบสองชั่วโมง อีกสิบสองช่องที่เป็นศูนย์คือที่เก็บที่จ่ายไปเพื่อไม่บอกอะไร

**ชั่วโมงตามเวลาร้าน** ด้วยเหตุผลเดียวกับวัน — บิลทุ่มนึงที่กรุงเทพคือตีหนึ่ง UTC
ถ้าแบ่งตาม UTC กราฟจะบอกว่าร้านขายดีที่สุดตอนเที่ยงคืน

**⚠️ วันเก่าที่สรุปไว้ก่อนหน้านี้ไม่มีคีย์ `hours`** และนี่คือจุดที่พลาดง่ายที่สุด:
ถ้านับเป็น "ชั่วโมงที่ไม่มีอะไรขาย" **วันที่ไม่มีใครบันทึกจะหน้าตาเหมือนวันที่ร้านเงียบ** เป๊ะ ๆ
→ `_hours()` แยกวันที่มีคีย์ออกจากวันที่ไม่มี แล้ว**บอกในผลลัพธ์ว่าคำนวณจากกี่วัน**
ถ้าไม่มีวันไหนมีเลย ตอบว่ายังดูไม่ได้ พร้อมบอกว่าวันที่สรุปหลังจากนี้จะมี

**ไม่ทำปุ่ม backfill** — จะให้วันเก่ามีข้อมูลต้องลบ rollup แล้วอ่านบิลใหม่ (เดือนละ ~3,000 read)
ไม่คุ้มกับการเพิ่มปุ่มซ่อมอีกอันบนหน้าตั้งค่าที่เพิ่งเก็บกวาดไป · ปล่อยให้มันเต็มไปข้างหน้าเอง
(ถ้าวันหนึ่งอยากได้จริง `delete_daily()` ช่วงนั้นแล้วเปิดดูก็สร้างใหม่เอง)

`group_by: "hour"` ดูได้แค่ `sales / bills / avg_bill` — ชั่วโมงไม่ได้เก็บว่าขายเมนูอะไร
และแยกตามช่องทางไม่ได้ ทั้งสองอย่างปฏิเสธพร้อมเหตุผล

### ยังตอบไม่ได้ (ไม่ใช่ปัญหาของผู้ช่วย — ระบบไม่ได้เก็บ)

- **ลูกค้า / พนักงาน / โต๊ะ** ไม่ได้เก็บเลย
- **เมนูแยกตามช่องทาง / เมนูแยกตามชั่วโมง** — rollup เก็บสามมิตินี้ขนานกัน ไม่ได้ไขว้กัน
- **พยากรณ์** ตอบได้แค่ "3 เดือนที่ผ่านมาเฉลี่ยเดือนละ X" ซึ่งเป็นข้อเท็จจริง ไม่ใช่การพยากรณ์

ทั้งหมดนี้เขียนไว้ใน `shop_query.describe()` ช่อง `ไม่ได้เก็บไว้` ซึ่งส่งให้โมเดลทุกครั้ง —
โมเดลจึงบอกได้ว่า *ไม่ได้เก็บ* แทนที่จะบอกว่า *ไม่มีข้อมูล* **และเพิ่มจุดบันทึกเมื่อไหร่
ผู้ช่วยตอบได้เองทันทีโดยไม่ต้องแก้โค้ดผู้ช่วยเลย**

---

## 8. ล็อกเวอร์ชันไลบรารี (สำคัญ)

**เกิดอะไรขึ้น:** deploy โดยที่โค้ดไม่เปลี่ยนเลย แล้ว login พังทั้งระบบ
```
InvalidArgument: 400 Invalid database id %28default%29
```
`%28default%29` คือ `(default)` ที่ถูก URL-encode ผิด — เป็นบั๊กของ
google-cloud-firestore/grpcio คู่ใหม่ ไม่ใช่โค้ดเรา

**สาเหตุ:** `requirements.txt` ใช้ `>=` ทั้งหมด ทุกครั้งที่ deploy
Northflank ติดตั้งเวอร์ชันล่าสุด = แอปคนละตัวกันทุกครั้งที่ ship
รอบนั้นได้ firestore 2.28.1 + grpcio 1.83.0 ที่เข้ากันไม่ได้

**แก้:** ล็อกทุกตัวด้วย `==` รวมถึง google-cloud-firestore / grpcio /
protobuf ที่ firebase-admin ดึงมาเอง (ตัวที่พังจริงคือคู่นี้)

**เวลาจะอัปเวอร์ชัน:** แก้เลขใน requirements.txt แล้ว deploy
ทดสอบ login + หน้ายอดขาย ก่อนถือว่าใช้ได้ — ไม่ใช่ปล่อยให้อัปเอง

หมายเหตุ: grpcio 1.68.1 จะขึ้น warning เรื่อง post-quantum crypto
ใช้งานได้ปกติ แต่ Google บอกว่าเดือน ต.ค. 2026 จะบังคับ >= 1.83.0
ถึงตอนนั้นต้องทดสอบว่าบั๊ก database id หายหรือยังก่อนอัป
